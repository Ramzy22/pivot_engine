---
phase: 04-data-input-api
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - pivot_engine/pivot_engine/data_input.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06

must_haves:
  truths:
    - "A pandas DataFrame passed to normalize_data_input loads without error"
    - "A polars DataFrame passed to normalize_data_input loads without error (optional dep)"
    - "An Ibis table expression passed to normalize_data_input loads without error"
    - "A connection string dict passed to normalize_data_input loads without error"
    - "All four types go through the same normalize_data_input(data, table_name, controller) signature"
    - "Unsupported types raise DataInputError with 'Supported types' in the message"
  artifacts:
    - path: "pivot_engine/pivot_engine/data_input.py"
      provides: "DataInputNormalizer class, DataInputError exception, normalize_data_input free function"
      exports:
        - DataInputNormalizer
        - DataInputError
        - normalize_data_input
  key_links:
    - from: "pivot_engine/pivot_engine/data_input.py"
      to: "pivot_engine/pivot_engine/controller.py"
      via: "controller.load_data_from_arrow(table_name, arrow_table)"
      pattern: "load_data_from_arrow"
    - from: "pivot_engine/pivot_engine/data_input.py"
      to: "pyarrow"
      via: "pa.Table.from_pandas(df, preserve_index=False)"
      pattern: "from_pandas.*preserve_index=False"
---

<objective>
Implement `pivot_engine/pivot_engine/data_input.py` to make all six tests in `tests/test_data_input.py` pass (TDD Green phase). This is the core normalization layer that accepts pandas, polars, Ibis, and connection string inputs and converts each to a PyArrow Table loaded into the controller.

Purpose: Deliver the observable behavior promised by requirements API-01 through API-06.
Output: data_input.py with DataInputNormalizer, DataInputError, and normalize_data_input. All 8 test cases GREEN.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-data-input-api/04-RESEARCH.md
@.planning/phases/04-data-input-api/04-01-SUMMARY.md

<interfaces>
<!-- Existing stable insertion point from pivot_engine/pivot_engine/controller.py:699 -->
```python
def load_data_from_arrow(
    self,
    table_name: str,
    arrow_table: pa.Table,
    register_checkpoint: bool = True
) -> None:
    # Loads arrow_table into the DuckDB/Ibis backend under table_name.
    # Clears the pivot cache after loading.
    ...
```

<!-- From tests/test_data_input.py (plan 01 output) — the contract to satisfy -->
```python
from pivot_engine.pivot_engine.data_input import DataInputNormalizer, DataInputError, normalize_data_input

normalize_data_input(data, table_name: str, controller) -> None
# data: pd.DataFrame | pl.DataFrame | ibis.Table | {"connection_string": str, "table": str} | pa.Table
# Raises DataInputError for unsupported types.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement data_input.py (GREEN)</name>
  <files>pivot_engine/pivot_engine/data_input.py</files>
  <behavior>
    - DataInputError is a subclass of TypeError
    - _is_pandas_dataframe: module starts with "pandas" and has "to_dict" attr (no top-level import)
    - _is_polars_dataframe: module starts with "polars" and has "to_arrow" attr (no top-level import)
    - _is_ibis_table: lazy import ibis.expr.types.Table, isinstance check
    - _is_connection_dict: isinstance(data, dict) and "connection_string" in data and "table" in data
    - normalize_data_input routes to correct converter, calls controller.load_data_from_arrow(table_name, arrow_table)
    - pandas path: pa.Table.from_pandas(df, preserve_index=False)
    - polars path: df.rechunk().to_arrow()
    - ibis path: expr.to_pyarrow()
    - connection string path: IbisBackend(connection_uri=...).con.table(table_name).to_pyarrow(); wrap ModuleNotFoundError as DataInputError with pip install hint
    - unsupported path: raise DataInputError with full "Supported types" list
  </behavior>
  <action>
    Create pivot_engine/pivot_engine/data_input.py with this exact structure:

    ```python
    """
    DataInputNormalizer — unified data ingestion for DashTanstackPivot.

    Accepts pandas DataFrame, polars DataFrame, Ibis table expression,
    connection string dict, or PyArrow Table and loads it into the pivot engine.

    API:
        normalize_data_input(data, table_name, controller) -> None
    """
    from __future__ import annotations

    import pyarrow as pa


    class DataInputError(TypeError):
        """Raised when the data= prop receives an unsupported or unloadable type."""
        pass


    def _is_pandas_dataframe(data) -> bool:
        """Detect pandas DataFrame without requiring pandas at module level."""
        return type(data).__module__.startswith("pandas") and hasattr(data, "to_dict")


    def _is_polars_dataframe(data) -> bool:
        """Detect polars DataFrame without requiring polars at module level."""
        return type(data).__module__.startswith("polars") and hasattr(data, "to_arrow")


    def _is_ibis_table(data) -> bool:
        """Detect an Ibis table expression via lazy import."""
        try:
            import ibis.expr.types as ibis_types  # noqa: PLC0415
            return isinstance(data, ibis_types.Table)
        except ImportError:
            return False


    def _is_connection_dict(data) -> bool:
        """Detect {"connection_string": ..., "table": ...} pattern."""
        return (
            isinstance(data, dict)
            and "connection_string" in data
            and "table" in data
        )


    def _pandas_to_arrow(df) -> pa.Table:
        """Convert pandas DataFrame to Arrow. preserve_index=False prevents index leakage."""
        return pa.Table.from_pandas(df, preserve_index=False)


    def _polars_to_arrow(df) -> pa.Table:
        """Convert polars DataFrame to Arrow. rechunk() is defensive for multi-chunk frames."""
        return df.rechunk().to_arrow()


    def _ibis_to_arrow(expr) -> pa.Table:
        """Convert Ibis table expression to Arrow via to_pyarrow()."""
        return expr.to_pyarrow()


    def _connection_dict_to_arrow(data: dict) -> pa.Table:
        """
        Load a table from an external database via connection string.

        data must have keys:
          - "connection_string": ibis-compatible URI (e.g. "duckdb://path.db", "postgres://...")
          - "table": table name in the target database
        """
        connection_string = data["connection_string"]
        table_name = data["table"]
        try:
            from .backends.ibis_backend import IbisBackend  # noqa: PLC0415
        except ImportError as exc:
            raise DataInputError(
                f"Could not import IbisBackend to handle connection_string input: {exc}. "
                f"Ensure ibis-framework is installed: pip install ibis-framework"
            ) from exc
        try:
            backend = IbisBackend(connection_uri=connection_string)
        except ModuleNotFoundError as exc:
            # e.g. "No module named 'psycopg2'" when postgres driver missing
            scheme = connection_string.split("://")[0] if "://" in connection_string else "unknown"
            raise DataInputError(
                f"Missing database driver for connection scheme '{scheme}': {exc}.\n"
                f"Install the required ibis backend: pip install ibis-framework[{scheme}]"
            ) from exc
        return backend.con.table(table_name).to_pyarrow()


    def _unsupported_type_error(data) -> DataInputError:
        type_str = f"{type(data).__module__}.{type(data).__name__}"
        return DataInputError(
            f"Unsupported data type: {type_str}.\n"
            f"Supported types:\n"
            f"  - pandas.DataFrame            (pip install pandas)\n"
            f"  - polars.DataFrame            (pip install polars)\n"
            f"  - ibis.Table expression       (pip install ibis-framework)\n"
            f"  - dict with keys 'connection_string' and 'table'\n"
            f"  - pyarrow.Table\n"
        )


    def normalize_data_input(data, table_name: str, controller) -> None:
        """
        Detect the type of *data*, convert it to a PyArrow Table, and load it into
        *controller* under *table_name*.

        Parameters
        ----------
        data : pd.DataFrame | pl.DataFrame | ibis.Table | dict | pa.Table
            The data source. Supported types are listed in the error message for
            unsupported inputs.
        table_name : str
            The name under which the table will be registered in the pivot engine.
        controller : ScalablePivotController (or any object with load_data_from_arrow)
            The pivot engine controller that owns the DuckDB/Ibis connection.

        Raises
        ------
        DataInputError
            If *data* is not one of the supported types.
        """
        if isinstance(data, pa.Table):
            arrow_table = data
        elif _is_pandas_dataframe(data):
            arrow_table = _pandas_to_arrow(data)
        elif _is_polars_dataframe(data):
            arrow_table = _polars_to_arrow(data)
        elif _is_ibis_table(data):
            arrow_table = _ibis_to_arrow(data)
        elif _is_connection_dict(data):
            arrow_table = _connection_dict_to_arrow(data)
        else:
            raise _unsupported_type_error(data)

        controller.load_data_from_arrow(table_name, arrow_table)


    class DataInputNormalizer:
        """
        Stateful wrapper around normalize_data_input that can be bound to a controller.

        Prefer the free function normalize_data_input() for simple one-shot use.
        This class is provided for adapters that need to bind a controller once.
        """

        def __init__(self, controller):
            self._controller = controller

        def load(self, data, table_name: str) -> None:
            """Normalize *data* and load it into the bound controller."""
            normalize_data_input(data, table_name, self._controller)
    ```

    IMPORTANT pitfall checks during implementation:
    - pandas: use `preserve_index=False` — prevents `__index_level_0__` leaking into schema
    - polars: use `df.rechunk().to_arrow()` — defensive against multi-chunk frames
    - ibis: use `expr.to_pyarrow()` — NOT `ibis.memtable(name=...)` (broken in ibis >= 9.x)
    - connection dict: wrap ModuleNotFoundError with pip install hint for missing backend driver

    Run tests after implementing:
    ```
    pytest tests/test_data_input.py -x -q
    ```
    All 8 test cases must pass (GREEN).
  </action>
  <verify>
    <automated>pytest tests/test_data_input.py -x -q</automated>
  </verify>
  <done>
    All 8 test cases in tests/test_data_input.py pass.
    `pytest tests/test_data_input.py -x -q` exits 0 with "8 passed".
  </done>
</task>

</tasks>

<verification>
File exists: `ls pivot_engine/pivot_engine/data_input.py`
All tests green: `pytest tests/test_data_input.py -v`
No index leakage: pandas test asserts schema has only {"region", "sales"}
Error messages actionable: unsupported type test asserts "Supported types" in message
</verification>

<success_criteria>
pivot_engine/pivot_engine/data_input.py created.
`pytest tests/test_data_input.py -x -q` exits 0 — all 8 tests pass.
All six API requirements (API-01 through API-06) have a GREEN test.
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-input-api/04-02-SUMMARY.md`
</output>
