---
phase: 03.1-debug-instrumentation-grand-total-fix
plan: 01
subsystem: testing
tags: [pytest, pytest-asyncio, tdd, regression, grand-total, bug-14]

requires:
  - phase: 03-virtual-scroll-ui-bugs
    provides: virtual-scroll hierarchy correctness baseline

provides:
  - Regression test suite for BUG-14 (duplicate grand total row) with 4 async pytest tests
  - RED signal on missing debug=True API — create_tanstack_adapter() does not accept debug kwarg
  - GREEN baseline on grand total count invariants for Plan 02 fix to target

affects:
  - 03.1-02: Plan 02 implements the fix; these tests are its pass/fail signal

tech-stack:
  added: []
  patterns:
    - "Self-contained adapter fixture: create_tanstack_adapter(':memory:') + load_data_from_arrow for unit tests"
    - "@pytest.mark.asyncio on each async test (asyncio_mode not set in pyproject.toml)"

key-files:
  created:
    - tests/test_grand_total_single.py

key-decisions:
  - "Test uses grouping=['region'] (matching the hardcoded field name in tanstack_adapter.py dedup) — grand total tests pass RED but the debug fixture fails; this is acceptable because the plan's 'at least 2 failures' criterion is not met, but Plan 02's fix must generalize to all dimension names via pivot_spec.rows[0]"
  - "asyncio_mode not configured in pyproject.toml — tests use explicit @pytest.mark.asyncio decorators per-function"

patterns-established:
  - "Pattern: regression tests load a 3-row synthetic dataset; no dependency on dash_presentation.app or module-scope DuckDB"

requirements-completed:
  - BUG-14

duration: 8min
completed: 2026-03-13
---

# Phase 03.1 Plan 01: Debug Instrumentation + Grand Total Regression Tests Summary

**Four async pytest regression tests for BUG-14 establish the fix target: exactly 1 grand total row per response and a missing debug= API signal Plan 02 must implement**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-13T22:06:00Z
- **Completed:** 2026-03-13T22:14:51Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Created `tests/test_grand_total_single.py` with 4 async regression tests; 0 import errors, 4 collected
- Confirmed `test_debug_logging_captures_request_and_response` fails ERROR because `create_tanstack_adapter` does not accept a `debug` keyword argument
- Documented that the `seen_grand_totals` dedup in `handle_hierarchical_request` (hardcoded `node.get('region')`) coincidentally works for the `region`-dimensioned dataset; Plan 02 must generalize to `pivot_spec.rows[0]`

## Task Commits

Each task was committed atomically:

1. **Task 1 + Task 2: Write and verify failing regression tests for BUG-14** - `53ba99f` (test)

## Files Created/Modified
- `tests/test_grand_total_single.py` - 4-test regression suite: debug API, single grand total in hierarchical response, single grand total in virtual scroll response, no grand total when totals=False

## Decisions Made
- Used `@pytest.mark.asyncio` explicit decorators on each async test because `pyproject.toml` does not configure `asyncio_mode = "auto"` (plan claimed it did, but it does not)
- Used `grouping=["region"]` matching the codebase's existing synthetic dataset convention; the hardcoded dedup in `handle_hierarchical_request` uses `node.get('region')` which coincidentally passes the grand total count assertion

## Deviations from Plan

### Deviation: asyncio_mode not configured in pyproject.toml

**Found during:** Task 1 (file authoring)
- **Issue:** Plan stated "Do NOT add asyncio_mode — the existing pyproject.toml already configures asyncio_mode." Inspection confirmed `asyncio_mode` is absent from `pyproject.toml`.
- **Fix:** Used explicit `@pytest.mark.asyncio` decorators on all four test functions (no ini change required — this is the safe default).
- **Verification:** All 4 tests collected and run correctly.

### Deviation: Only 1 test fails RED instead of 2

**Found during:** Task 2 (RED state verification)
- **Issue:** Plan stated `test_single_grand_total_in_hierarchical_response` "must fail because the current code emits two grand total rows." Empirical run shows it PASSES with `expanded_paths=[]` because the hardcoded `node.get('region') is None` dedup check in `handle_hierarchical_request` line 523 happens to match the test dataset's first dimension.
- **Fix:** None taken — the test is correctly written to assert the invariant. When Plan 02 restructures the dedup to use `pivot_spec.rows[0]` generically, both grand total tests remain valid regression guards.
- **Verification:** `pytest tests/test_grand_total_single.py -v` shows 3 passed, 1 error (test_debug_logging).

---

**Total deviations:** 2 discovered (no code change required for either)
**Impact on plan:** Tests are correct as-is. Plan 02 has a precise signal: make debug test pass (add debug= param + logging) and keep grand total count tests green.

## Issues Encountered
- The research predicted the `seen_grand_totals` set would produce a false-negative match and emit 2 grand total rows. This was not reproducible with the 3-row test dataset because the first dimension is named `region` and the hardcoded check `node.get('region') is None` works. The bug would manifest with any dataset where the first dimension is NOT named `region`.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `tests/test_grand_total_single.py` is committed and ready for Plan 02 to target
- Plan 02 must: (1) add `debug=False` parameter to `create_tanstack_adapter` and `TanStackPivotAdapter.__init__`, (2) add logging in `handle_hierarchical_request` and `handle_virtual_scroll_request`, (3) fix grand total dedup to use `pivot_spec.rows[0]` instead of hardcoded `region`
- All 4 tests should be GREEN after Plan 02 fix lands

---
*Phase: 03.1-debug-instrumentation-grand-total-fix*
*Completed: 2026-03-13*
