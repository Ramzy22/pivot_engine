---
phase: 02-data-correctness-bugs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_visual_totals.py
  - tests/test_frontend_contract.py
  - tests/test_frontend_filters.py
  - pivot_engine/tests/test_controller.py
autonomous: true
requirements:
  - BUG-01
  - BUG-02
  - BUG-03
  - BUG-04
  - BUG-05
  - BUG-06

must_haves:
  truths:
    - "Phase 2 has failing or proving regression tests for every roadmap bug before backend fixes start"
    - "Totals tests cover sum, avg, count, min, and max instead of only sum"
    - "Adapter-level tests exercise filter/sort/expansion sequences rather than isolated one-shot requests"
  artifacts:
    - path: "tests/test_visual_totals.py"
      provides: "Grand total regression coverage across aggregation types"
      contains: "avg"
    - path: "tests/test_frontend_contract.py"
      provides: "State persistence regression coverage across hierarchy interactions"
      contains: "refresh"
    - path: "pivot_engine/tests/test_controller.py"
      provides: "Controller-level regressions for totals and server-side sort semantics"
      contains: "sort"
  key_links:
    - from: "tests/test_frontend_contract.py"
      to: "pivot_engine/pivot_engine/tanstack_adapter.py"
      via: "adapter request -> PivotSpec -> response assertions"
      pattern: "handle_request|handle_hierarchical_request"
---

<objective>
Add the missing regression coverage for Phase 2 before changing backend behavior. The current suite proves only narrow happy paths. Phase 2 execution needs explicit tests for the contract in ROADMAP.md: grand totals correctness, dynamic columns completeness, and filter/sort persistence through hierarchy and refresh-like request sequences.

Purpose: give downstream plans precise failing/proving tests so backend fixes are evidence-driven instead of speculative.
Output: expanded pytest coverage in the existing totals, controller, and frontend contract test files.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-correctness-bugs/02-RESEARCH.md
@.planning/phases/02-data-correctness-bugs/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend totals regression coverage</name>
  <files>tests/test_visual_totals.py, pivot_engine/tests/test_controller.py</files>
  <action>
    Add explicit tests that assert correct grand total behavior for `sum`, `avg`, `count`, `min`, and `max`.

    Requirements:
    - Cover plain totals and post-aggregation-filter/visual-totals scenarios.
    - Include at least one case where AVG would be wrong if implemented as "sum of averages".
    - Assert both value correctness and the presence of a single grand total row.

    Keep the tests small and data-driven. Prefer a shared fixture/helper over copy-pasting five near-identical tests.
  </action>
  <verify>
    <automated>python -m pytest tests/test_visual_totals.py pivot_engine/tests/test_controller.py -q --tb=line</automated>
  </verify>
  <done>Totals regressions exist for all five aggregate types and fail against incorrect roll-up behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add request-sequence regressions for adapter state persistence</name>
  <files>tests/test_frontend_contract.py, tests/test_frontend_filters.py</files>
  <action>
    Add adapter-level tests that simulate realistic sequences instead of isolated calls:

    - filter -> expand -> re-request
    - filter -> sort -> re-request
    - filter -> virtual-scroll/hierarchical request fallback
    - repeated requests after data/filter changes for dynamic column completeness

    Assertions must be on returned payloads, not implementation details. Verify the same filter and sort intent survives across the sequence and that the returned column set stays complete/non-sparse.
  </action>
  <verify>
    <automated>python -m pytest tests/test_frontend_contract.py tests/test_frontend_filters.py -q --tb=line</automated>
  </verify>
  <done>Adapter regressions now cover expansion, sort, refresh-like re-request, and dynamic-column continuity.</done>
</task>

</tasks>

<verification>
Run the Phase 2 quick suite:

`python -m pytest tests/test_visual_totals.py tests/test_frontend_contract.py tests/test_frontend_filters.py pivot_engine/tests/test_controller.py test_expand_all_backend.py -q --tb=line`

Expected: the new tests are collected and give a clear red/green signal for each targeted bug.
</verification>

<success_criteria>
- Each Phase 2 requirement is represented by at least one explicit regression assertion
- No new tests depend on manual inspection or print debugging
- Existing Phase 1 baseline tests still pass
- The new tests are focused enough to point at a single subsystem when they fail
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-correctness-bugs/02-01-SUMMARY.md` following the summary template.
</output>
