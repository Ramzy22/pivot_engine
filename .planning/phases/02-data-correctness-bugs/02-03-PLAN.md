---
phase: 02-data-correctness-bugs
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pivot_engine/pivot_engine/planner/ibis_planner.py
  - pivot_engine/pivot_engine/scalable_pivot_controller.py
  - pivot_engine/pivot_engine/tanstack_adapter.py
  - tests/test_frontend_contract.py
autonomous: true
requirements:
  - BUG-03
  - BUG-04

must_haves:
  truths:
    - "Discovered pivot columns are complete and non-sparse after data/filter changes"
    - "Repeated refresh-like requests produce a consistent column set for the same request state"
    - "Column discovery caching cannot leak stale column keys across incompatible request states"
  artifacts:
    - path: "pivot_engine/pivot_engine/tanstack_adapter.py"
      provides: "Stable request-to-spec mapping for dynamic column discovery"
      contains: "column_cursor"
    - path: "pivot_engine/pivot_engine/scalable_pivot_controller.py"
      provides: "Correct cache/invalidation behavior for discovery and pivot result execution"
      contains: "_execute_topn_pivot_async"
  key_links:
    - from: "pivot_engine/pivot_engine/scalable_pivot_controller.py"
      to: "tests/test_frontend_contract.py"
      via: "top-N discovery path + repeated request assertions"
      pattern: "_execute_topn_pivot_async"
---

<objective>
Fix dynamic pivot-column discovery so the UI gets the complete, correct column set after data changes, filter changes, and repeated refresh-like requests.

Purpose: close BUG-03 and BUG-04 at the discovery/cache boundary instead of hiding stale columns in the adapter response.
Output: stable dynamic-column behavior with passing repeated-request regressions.
</objective>

<context>
@.planning/phases/02-data-correctness-bugs/02-RESEARCH.md
@.planning/phases/02-data-correctness-bugs/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix column discovery completeness</name>
  <files>pivot_engine/pivot_engine/planner/ibis_planner.py, pivot_engine/pivot_engine/scalable_pivot_controller.py</files>
  <action>
    Audit the top-N discovery path and fix any behavior that returns incomplete or sparse discovered columns after the underlying request state changes.

    Focus areas:
    - request filters applied to discovery query
    - full request state represented in discovery/pivot cache boundaries
    - discovered column ordering remaining deterministic

    Prefer explicit invalidation or stronger cache keys over ad-hoc response patching.
  </action>
  <verify>
    <automated>python -m pytest tests/test_frontend_contract.py -q --tb=line</automated>
  </verify>
  <done>Column discovery regressions prove the returned column set is complete for the active request state.</done>
</task>

<task type="auto">
  <name>Task 2: Fix refresh consistency across repeated requests</name>
  <files>pivot_engine/pivot_engine/tanstack_adapter.py, pivot_engine/pivot_engine/scalable_pivot_controller.py</files>
  <action>
    Ensure repeated requests with the same request state produce the same discovered column set, while meaningful request changes produce the newly correct set.

    Requirements:
    - same request => same columns
    - changed filters/data => updated columns
    - no duplicate or dropped discovered columns in response formatting
  </action>
  <verify>
    <automated>python -m pytest tests/test_frontend_contract.py tests/test_frontend_filters.py -q --tb=line</automated>
  </verify>
  <done>Refresh-like request regressions pass and discovered columns remain stable without going stale.</done>
</task>

</tasks>

<verification>
Run the dynamic-column subset:

`python -m pytest tests/test_frontend_contract.py tests/test_frontend_filters.py -q --tb=line`

Expected: repeated-request assertions pass and no sparse/duplicate discovered columns remain.
</verification>

<success_criteria>
- Dynamic column regressions pass after filter/data changes
- Repeated same-state requests are stable
- Cache behavior is explicit and testable
- No frontend-only patch is required to hide backend discovery bugs
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-correctness-bugs/02-03-SUMMARY.md` following the summary template.
</output>
