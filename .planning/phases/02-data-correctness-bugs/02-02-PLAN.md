---
phase: 02-data-correctness-bugs
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pivot_engine/pivot_engine/planner/ibis_planner.py
  - pivot_engine/pivot_engine/scalable_pivot_controller.py
  - tests/test_visual_totals.py
  - pivot_engine/tests/test_controller.py
autonomous: true
requirements:
  - BUG-01
  - BUG-06

must_haves:
  truths:
    - "Grand totals are correct for sum, avg, count, min, and max"
    - "Server-side sort order is applied to the actual returned pivot result"
    - "Standard controller execution returns the finalized table path instead of bypassing it"
  artifacts:
    - path: "pivot_engine/pivot_engine/planner/ibis_planner.py"
      provides: "Correct aggregation semantics for totals and server-side sort behavior"
      contains: "order_by"
    - path: "pivot_engine/pivot_engine/scalable_pivot_controller.py"
      provides: "Correct finalized result return path"
      contains: "final_table"
  key_links:
    - from: "pivot_engine/pivot_engine/planner/ibis_planner.py"
      to: "tests/test_visual_totals.py"
      via: "aggregate semantics for totals/visual totals"
      pattern: "_plan_standard|build_pivot_query_from_columns"
---

<objective>
Fix the backend correctness bugs around totals and sort semantics. This plan should make the server return the correct numbers and ordering before any dynamic-column or hierarchical persistence work builds on top of it.

Purpose: stabilize the core query engine for BUG-01 and BUG-06.
Output: corrected planner/controller behavior with passing totals and sort regressions.
</objective>

<context>
@.planning/phases/02-data-correctness-bugs/02-RESEARCH.md
@.planning/phases/02-data-correctness-bugs/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix totals semantics in planner paths</name>
  <files>pivot_engine/pivot_engine/planner/ibis_planner.py</files>
  <action>
    Update planner totals behavior so grand totals remain mathematically correct for all required aggregate types.

    Requirements:
    - Visual totals must remain correct under post-aggregation filtering.
    - AVG must not be derived by summing pre-averaged group values.
    - COUNT/MIN/MAX must preserve their correct roll-up semantics.

    Keep the implementation readable. If the current visual-totals branch cannot support AVG safely with the existing shape, refactor the branch so the tests prove the correct result instead of preserving the current shortcut.
  </action>
  <verify>
    <automated>python -m pytest tests/test_visual_totals.py pivot_engine/tests/test_controller.py -q --tb=line</automated>
  </verify>
  <done>Totals tests for all aggregate types pass without special-casing only sum.</done>
</task>

<task type="auto">
  <name>Task 2: Fix finalized result return path and server-side sort persistence</name>
  <files>pivot_engine/pivot_engine/scalable_pivot_controller.py, pivot_engine/pivot_engine/planner/ibis_planner.py</files>
  <action>
    Fix the standard execution path so the controller returns the finalized table, not the pre-finalized intermediate result.

    Then fix server-side sorting so user sort state still applies in pivoted-column mode and after repeated requests.

    Important:
    - Do not paper over failures in the adapter.
    - The planner/controller should enforce the authoritative server ordering.
    - Preserve stable default ordering when no explicit sort is requested.
  </action>
  <verify>
    <automated>python -m pytest tests/test_frontend_contract.py pivot_engine/tests/test_controller.py -q --tb=line</automated>
  </verify>
  <done>The result path is finalized correctly and sort regressions prove server-side order stability.</done>
</task>

</tasks>

<verification>
Run the quick suite plus the totals/controller subset:

`python -m pytest tests/test_visual_totals.py tests/test_frontend_contract.py pivot_engine/tests/test_controller.py -q --tb=line`

Expected: totals and sort regressions pass without relying on client-side reordering.
</verification>

<success_criteria>
- Grand total assertions pass for all five aggregate types
- Explicit sort requests survive repeated server calls
- No adapter-only workaround is required to get correct order
- Existing controller pagination tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-correctness-bugs/02-02-SUMMARY.md` following the summary template.
</output>
