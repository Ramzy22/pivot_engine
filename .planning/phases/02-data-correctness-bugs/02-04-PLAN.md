---
phase: 02-data-correctness-bugs
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - pivot_engine/pivot_engine/tanstack_adapter.py
  - pivot_engine/pivot_engine/hierarchical_scroll_manager.py
  - pivot_engine/pivot_engine/scalable_pivot_controller.py
  - tests/test_frontend_contract.py
  - test_expand_all_backend.py
autonomous: true
requirements:
  - BUG-02
  - BUG-05
  - BUG-06

must_haves:
  truths:
    - "Filter state persists through expansion, sort changes, and virtual-scroll requests"
    - "Grand total rows remain singular and stable across hierarchy/scroll interactions"
    - "Virtual-scroll fallback does not silently reset filter or sort state"
  artifacts:
    - path: "pivot_engine/pivot_engine/tanstack_adapter.py"
      provides: "Stable hierarchical and virtual-scroll request handling"
      contains: "handle_virtual_scroll_request"
    - path: "tests/test_frontend_contract.py"
      provides: "End-to-end persistence regressions for hierarchy interactions"
      contains: "expanded_paths"
  key_links:
    - from: "pivot_engine/pivot_engine/tanstack_adapter.py"
      to: "pivot_engine/pivot_engine/hierarchical_scroll_manager.py"
      via: "hierarchical/virtual-scroll request path"
      pattern: "handle_hierarchical_request|handle_virtual_scroll_request"
---

<objective>
Close the remaining integration bugs around hierarchy expansion, virtual scroll, and state persistence. By this point totals and dynamic columns should already be correct; this plan makes sure those correct results survive realistic interaction sequences.

Purpose: satisfy BUG-02 and BUG-05, and finish the user-visible part of BUG-06.
Output: stable hierarchical and virtual-scroll behavior under filter/sort/expand sequences.
</objective>

<context>
@.planning/phases/02-data-correctness-bugs/02-RESEARCH.md
@.planning/phases/02-data-correctness-bugs/02-02-PLAN.md
@.planning/phases/02-data-correctness-bugs/02-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hierarchy expansion persistence under filter and sort changes</name>
  <files>pivot_engine/pivot_engine/tanstack_adapter.py, tests/test_frontend_contract.py, test_expand_all_backend.py</files>
  <action>
    Fix any request-path bugs where expansion state, filter state, or sort state are lost when the UI performs a hierarchy interaction.

    Cover these sequences with tests and implementation:
    - filter -> expand
    - sort -> expand
    - filter -> sort -> expand
    - expand-all with active filters

    Grand total behavior must stay stable throughout those sequences.
  </action>
  <verify>
    <automated>python -m pytest tests/test_frontend_contract.py test_expand_all_backend.py -q --tb=line</automated>
  </verify>
  <done>Hierarchy interactions preserve active filter/sort intent and do not duplicate or lose the grand total row.</done>
</task>

<task type="auto">
  <name>Task 2: Fix virtual-scroll continuity and fallback behavior</name>
  <files>pivot_engine/pivot_engine/tanstack_adapter.py, pivot_engine/pivot_engine/hierarchical_scroll_manager.py, pivot_engine/pivot_engine/scalable_pivot_controller.py</files>
  <action>
    Verify virtual-scroll requests preserve the same effective spec as non-scroll requests. If the virtual-scroll path fails and falls back, the fallback must return the same filter/sort semantics instead of resetting the request.

    Keep the behavior deterministic:
    - same request state => same rows/ordering semantics
    - fallback path => same logical result set
  </action>
  <verify>
    <automated>python -m pytest tests/test_frontend_contract.py tests/test_frontend_filters.py test_expand_all_backend.py -q --tb=line</automated>
  </verify>
  <done>Virtual-scroll and fallback requests preserve filter/sort state and keep the grand total stable.</done>
</task>

</tasks>

<verification>
Run the Phase 2 quick suite, then the full suite:

`python -m pytest tests/test_visual_totals.py tests/test_frontend_contract.py tests/test_frontend_filters.py pivot_engine/tests/test_controller.py test_expand_all_backend.py -q --tb=line`

`python -m pytest tests/ test_expand_all_backend.py test_filtering.py pivot_engine/tests/ pivot_engine/test_arrow_conversion.py pivot_engine/test_async_changes.py pivot_engine/test_cursor_simple.py pivot_engine/test_scalable_async_changes.py pivot_engine/test_totals_demo.py -v --tb=short`

Expected: filter/sort/expand/scroll sequences remain stable and the Phase 1 baseline stays green.
</verification>

<success_criteria>
- Persistence regressions pass across hierarchy and virtual scroll
- Grand total row remains singular and stable across request sequences
- Full suite remains green
- Phase 2 requirements BUG-01 through BUG-06 are all covered by implemented plans
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-correctness-bugs/02-04-SUMMARY.md` following the summary template.
</output>
