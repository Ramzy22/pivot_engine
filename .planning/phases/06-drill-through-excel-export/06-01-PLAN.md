---
phase: 06-drill-through-excel-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/test_drill_through.py
  - tests/test_export.py
autonomous: true
requirements: [DRILL-03, DRILL-04, DRILL-05, DRILL-06, EXPORT-05]

must_haves:
  truths:
    - "REQUIREMENTS.md contains EXPORT-05 under the Excel Export section"
    - "tests/test_drill_through.py exists with failing RED tests for DRILL-03 through DRILL-06"
    - "tests/test_export.py exists with failing RED tests for EXPORT-02 through EXPORT-05"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "EXPORT-05 requirement entry + traceability row"
    - path: "tests/test_drill_through.py"
      provides: "RED pytest tests for endpoint behaviour, pagination, sort/filter, coordinate filters"
    - path: "tests/test_export.py"
      provides: "RED pytest tests for xlsx multi-level headers, row indentation, totals inclusion, format threshold"
  key_links:
    - from: "tests/test_drill_through.py"
      to: "dash_presentation/app.py /api/drill-through route"
      via: "Flask test client GET request"
      pattern: "client.get.*api/drill-through"
    - from: "tests/test_export.py"
      to: "pivot_engine/pivot_engine/scalable_pivot_controller.py get_drill_through_data"
      via: "asyncio.run or pytest-asyncio"
      pattern: "get_drill_through_data"
---

<objective>
Add the missing EXPORT-05 requirement to REQUIREMENTS.md and write failing (RED) test scaffolds for the drill-through endpoint and export helper logic.

Purpose: All Phase 6 backend behaviour must be test-driven. Tests must fail before implementation so GREEN state is meaningful. Also records EXPORT-05 (the xlsx/csv 500k threshold) which was absent from REQUIREMENTS.md despite being in the roadmap success criteria.

Output: Updated REQUIREMENTS.md, tests/test_drill_through.py (RED), tests/test_export.py (RED).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-drill-through-excel-export/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EXPORT-05 to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Open .planning/REQUIREMENTS.md and make two targeted edits:

    1. Under the `### Excel Export` section, after the EXPORT-04 line, insert:
       ```
       - [ ] **EXPORT-05**: "Export" button downloads .xlsx when pivot row count is ≤ 500,000; downloads .csv above that threshold; button label and icon update to reflect the format that will be produced
       ```

    2. In the `## Traceability` table, after the EXPORT-04 row, insert:
       ```
       | EXPORT-05 | Phase 6 - Drill-Through & Excel Export | Pending |
       ```

    3. Update the Coverage count line from:
       `v1 requirements: 57 total ...`
       to reflect 58 total (or simply leave the count note as-is and update the last-updated date to today).

    Do NOT change any other lines.
  </action>
  <verify>
    <automated>python -c "txt = open('.planning/REQUIREMENTS.md').read(); assert 'EXPORT-05' in txt, 'EXPORT-05 missing'"</automated>
  </verify>
  <done>REQUIREMENTS.md contains EXPORT-05 entry in the Excel Export section and in the Traceability table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED test scaffolds for drill-through and export</name>
  <files>tests/test_drill_through.py, tests/test_export.py</files>
  <behavior>
    test_drill_through.py:
    - test_endpoint_returns_rows: GET /api/drill-through?table=sales_data&row_path=North&row_fields=region&page=0&page_size=10 via Flask test client returns HTTP 200 and JSON with "rows" key containing a list
    - test_pagination: page=0 and page=1 return different rows (non-overlapping offset); both return at most page_size rows
    - test_sort_and_filter: passing sort_col=sales&sort_dir=desc returns rows; passing filter=USA returns rows where at least one field contains "USA"
    - test_coordinate_filters: passing row_path=North|||USA&row_fields=region,country returns only rows where region="North" AND country="USA"
    - test_total_rows_count_in_response: response JSON contains "total_rows" key with an integer value >= number of rows returned

    test_export.py:
    - test_get_drill_through_data_pagination: asyncio.run(controller.get_drill_through_data(spec, [], limit=5, offset=0)) returns exactly 5 rows; offset=5 returns the next 5 (not duplicates of first 5)
    - test_get_drill_through_data_coord_filters: filters=[{"field":"region","op":"=","value":"North"}] returns only rows with region="North"
    - test_get_drill_through_data_sort (DRILL-05 backend portion): after Plan 02 adds sort support, passing sort=[{"col":"sales","dir":"desc"}] returns rows in descending sales order

    Note: tests/test_export.py is a misnomer for Phase 6 — it tests the backend get_drill_through_data method (the JS export helper logic is not testable in pytest). Name the file test_export.py to match the research plan but keep tests focused on the Python layer.

    All tests MUST fail (RED) before the endpoint and sort extension exist. Use `pytest.importorskip` to guard against missing modules gracefully — tests should produce ERRORS or FAILURES, not collection errors.

    In test_drill_through.py use the Flask test client pattern:
    ```python
    import sys, os
    sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..', 'pivot_engine')))
    sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..', 'dash_presentation')))

    import pytest

    @pytest.fixture(scope="module")
    def client():
        from app import app, get_adapter
        get_adapter()  # pre-warm data
        app.config['TESTING'] = True
        with app.server.test_client() as c:
            yield c
    ```

    In test_export.py use asyncio.run or @pytest.mark.asyncio per the pyproject.toml pattern (explicit marks, no asyncio_mode=auto):
    ```python
    import sys, os
    sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..', 'pivot_engine')))
    import asyncio, pytest
    from pivot_engine.pivot_engine.scalable_pivot_controller import ScalablePivotController
    from pivot_engine.pivot_engine.types.pivot_spec import PivotSpec, Measure
    import pyarrow as pa
    ```
  </behavior>
  <action>
    Create tests/test_drill_through.py with the five test functions listed in the behavior block. Each test should assert specific behaviour that CANNOT pass until Plan 02 is complete:
    - The /api/drill-through route does not exist yet → 404 on the test client → test_endpoint_returns_rows fails with AssertionError.
    - total_rows key will be absent until the endpoint is implemented → test_total_rows_count_in_response fails.

    Create tests/test_export.py with the three test functions listed in the behavior block. The sort test should be skipped with pytest.skip("sort param not yet implemented — Plan 02 adds it") so it is skipped (not erroring) until Plan 02.

    Use small in-memory test data (10–20 rows using pyarrow) inside a module-scoped fixture so tests run fast.

    Run `python -m pytest tests/test_drill_through.py tests/test_export.py -x` and confirm tests are collected but fail (not collection errors).
  </action>
  <verify>
    <automated>cd C:/Users/ramzy/Downloads/pivot_engine_skeleton && python -m pytest tests/test_drill_through.py tests/test_export.py --tb=no -q 2>&1 | grep -E "FAILED|ERROR|passed|error" | head -20</automated>
  </verify>
  <done>Both test files are collected without import errors. At least one test in each file is in FAILED or ERROR state (not all passing), confirming RED state before implementation.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/test_drill_through.py tests/test_export.py --tb=short -q` collects all tests and reports failures/errors (not collection errors). EXPORT-05 is present in .planning/REQUIREMENTS.md.
</verification>

<success_criteria>
1. .planning/REQUIREMENTS.md has EXPORT-05 entry with description matching the roadmap success criterion 6
2. tests/test_drill_through.py exists with ≥5 test functions; all fail (RED) or error due to missing route
3. tests/test_export.py exists with ≥2 active test functions; sort test is skipped; pagination/filter tests fail (RED) or pass only if the controller already supports them
4. No collection errors — pytest can import both test modules cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-drill-through-excel-export/06-01-SUMMARY.md`
</output>
