---
phase: 01-test-audit-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .planning/phases/01-test-audit-baseline/audit_raw.txt
autonomous: true
requirements:
  - QUAL-01

must_haves:
  truths:
    - "Every pytest-runnable test file has been executed and its result recorded"
    - "The raw audit output is saved to a file for post-processing in plan 04"
    - "The 50% gate check has been evaluated — if >50% fail, executor stops and surfaces the count for human triage"
  artifacts:
    - path: ".planning/phases/01-test-audit-baseline/audit_raw.txt"
      provides: "Raw pytest output — pass/fail/error for every collected test"
  key_links:
    - from: "pytest"
      to: ".planning/phases/01-test-audit-baseline/audit_raw.txt"
      via: "tee command capturing stdout+stderr"
      pattern: "tee.*audit_raw\\.txt"
---

<objective>
Run the full pytest suite from the repo root (across all four test locations) and save the raw output. Evaluate the 50% gate: if more than half of collected tests fail, stop and surface the failure count — something systemic is wrong and the user must triage before fixing begins.

Purpose: Establish the true baseline count of passing, failing, and erroring tests. All fix work in plans 03 and 04 is driven by this output.
Output: `.planning/phases/01-test-audit-baseline/audit_raw.txt` containing the complete pytest run.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-test-audit-baseline/01-RESEARCH.md
@.planning/phases/01-test-audit-baseline/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key facts the executor needs. Extracted from RESEARCH.md. -->

pytest-runnable files (confirmed by research):
- pivot_engine/tests/ — 16 files (11 collecting OK, 5 with collection errors for missing modules)
- tests/ (repo root) — 4 files (all OK)
- test_expand_all_backend.py (repo root) — pytest file
- test_filtering.py (repo root) — pytest file (needs conftest.py path fix, done in plan 01)

Confirmed NOT pytest-runnable (standalone scripts — do NOT pass to pytest):
- test_expand_all.py, test_fix_verification.py, test_flat_final.py, test_flat_output.py,
  test_ifelse.py, test_tanstack_communication.py, test_virtual_scroll.py,
  test_virtual_scroll_pivot.py (all at repo root)
- pivot_engine/test_backend.py, test_connection.py, test_exact_issue.py,
  test_final_implementation.py, test_hierarchical_async.py, test_materialized_hierarchy.py,
  test_with_clear_cache.py (all in pivot_engine/ root)

Known collection errors (5 files — will be fixed in plan 03):
- pivot_engine/tests/test_advanced_planning.py — missing planner.sql_planner
- pivot_engine/tests/test_config_main.py — missing ScalablePivotApplication
- pivot_engine/tests/test_diff_engine_enhancements.py — missing MultiDimensionalTilePlanner
- pivot_engine/tests/test_features_impl.py — missing httpx (FIXED by plan 01)
- pivot_engine/tests/test_microservices.py — missing pivot_microservices
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run full pytest audit and save raw output</name>
  <files>.planning/phases/01-test-audit-baseline/audit_raw.txt</files>
  <action>
    From the repo root, run pytest across all pytest-runnable test locations. Use `--no-header -v --tb=short` for verbose, readable output. Do NOT use `-x` (fail-fast) — capture all failures.

    Also include the five standalone pytest files from pivot_engine/ root that are pytest-runnable:

    ```
    python -m pytest \
      tests/ \
      test_expand_all_backend.py \
      test_filtering.py \
      pivot_engine/tests/ \
      pivot_engine/test_arrow_conversion.py \
      pivot_engine/test_async_changes.py \
      pivot_engine/test_cursor_simple.py \
      pivot_engine/test_scalable_async_changes.py \
      pivot_engine/test_totals_demo.py \
      -v --tb=short --no-header \
      2>&1 | tee .planning/phases/01-test-audit-baseline/audit_raw.txt
    ```

    After the run completes, extract the summary line from audit_raw.txt:
    ```
    grep -E "passed|failed|error" .planning/phases/01-test-audit-baseline/audit_raw.txt | tail -5
    ```

    Count total collected tests and total failing tests (failures + errors). If failures+errors > 50% of collected, STOP and output:
    ```
    GATE TRIGGERED: {failures+errors}/{total} tests failing (>{50%} threshold).
    Systemic issue suspected. Human triage required before fix work begins.
    Raw output: .planning/phases/01-test-audit-baseline/audit_raw.txt
    ```

    If gate passes (<=50% failing), proceed normally and create the summary.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-test-audit-baseline/audit_raw.txt && echo "audit_raw.txt exists" && wc -l .planning/phases/01-test-audit-baseline/audit_raw.txt</automated>
  </verify>
  <done>
    audit_raw.txt exists and contains the complete pytest run output including the final summary line (e.g., "X passed, Y failed, Z errors").
    If gate was not triggered: Summary line is visible and failure count is ≤50% of total collected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run standalone scripts and record their pass/fail</name>
  <files>.planning/phases/01-test-audit-baseline/scripts_raw.txt</files>
  <action>
    The 15 standalone script-style test_*.py files cannot be collected by pytest. Run each with `python` and capture the exit code. A non-zero exit code or uncaught exception = failure. Output results to scripts_raw.txt.

    Run from the repo root:
    ```
    python -m pytest --collect-only -q pivot_engine/test_backend.py 2>&1 | head -3
    ```

    Actually run each script file directly and note the result:

    Root-level scripts (run from repo root):
    ```
    for f in test_expand_all.py test_fix_verification.py test_flat_final.py test_flat_output.py test_ifelse.py test_tanstack_communication.py test_virtual_scroll.py test_virtual_scroll_pivot.py; do
      echo "--- $f ---" >> .planning/phases/01-test-audit-baseline/scripts_raw.txt
      timeout 30 python "$f" >> .planning/phases/01-test-audit-baseline/scripts_raw.txt 2>&1
      echo "EXIT: $?" >> .planning/phases/01-test-audit-baseline/scripts_raw.txt
    done
    ```

    pivot_engine/ scripts (run from pivot_engine/ directory):
    ```
    cd pivot_engine
    for f in test_backend.py test_connection.py test_exact_issue.py test_final_implementation.py test_hierarchical_async.py test_materialized_hierarchy.py test_with_clear_cache.py; do
      echo "--- $f ---" >> ../.planning/phases/01-test-audit-baseline/scripts_raw.txt
      timeout 30 python "$f" >> ../.planning/phases/01-test-audit-baseline/scripts_raw.txt 2>&1
      echo "EXIT: $?" >> ../.planning/phases/01-test-audit-baseline/scripts_raw.txt
    done
    cd ..
    ```

    These scripts are informational only — they were never proper pytest tests. Document their results but do NOT attempt to convert them to pytest in this plan. The CONTEXT.md decision is that standalone scripts that cannot be pytest-ified are documented as "script tests" in the baseline report.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-test-audit-baseline/scripts_raw.txt && echo "scripts_raw.txt exists" && grep -c "EXIT:" .planning/phases/01-test-audit-baseline/scripts_raw.txt</automated>
  </verify>
  <done>scripts_raw.txt exists and contains 15 "EXIT:" entries — one for each standalone script.</done>
</task>

</tasks>

<verification>
Verify both output files exist and are non-empty:
```
ls -la .planning/phases/01-test-audit-baseline/audit_raw.txt .planning/phases/01-test-audit-baseline/scripts_raw.txt
```

Verify the audit contains the pytest summary line:
```
grep -E "=+ .* =+" .planning/phases/01-test-audit-baseline/audit_raw.txt | tail -3
```
</verification>

<success_criteria>
- `audit_raw.txt` exists with at least 50 lines of pytest output and a final summary line
- `scripts_raw.txt` exists with 15 EXIT: entries
- 50% gate evaluated — either plan continues normally or a clear GATE TRIGGERED message is output
- No production code was modified
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-audit-baseline/01-02-SUMMARY.md` following the summary template.
Include the gate result (passed/triggered) and the raw counts (X passed, Y failed, Z errors).
</output>
