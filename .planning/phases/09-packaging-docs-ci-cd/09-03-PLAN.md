---
phase: 09-packaging-docs-ci-cd
plan: 03
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - scripts/ci/check_tag_version.py
  - scripts/ci/verify_bundle_contract.py
autonomous: true
requirements:
  - PKG-03
  - CI-01
  - CI-02
  - CI-03

must_haves:
  truths:
    - "Every push runs Python test gates, including multi-instance isolation tests"
    - "Every push runs JS build and bundle artifact contract checks"
    - "Release workflow triggers only on semantic version tags and validates tag/version parity"
    - "PyPI publish runs only after build, test, and artifact checks pass"
    - "CI treats multi-instance safety as a release gate, not optional documentation"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Push/PR CI for Python tests, JS build, package smoke, and isolation gates"
      contains: "pytest"
    - path: ".github/workflows/release.yml"
      provides: "Tag-triggered publish workflow with semver checks and PyPI publish action"
      contains: "gh-action-pypi-publish"
    - path: "scripts/ci/check_tag_version.py"
      provides: "Deterministic semantic-version guard against tag/package mismatch"
      exports: ["main"]
    - path: "scripts/ci/verify_bundle_contract.py"
      provides: "Artifact contract check for expected JS bundle outputs"
      exports: ["main"]
  key_links:
    - from: ".github/workflows/ci.yml python job"
      to: "tests/test_multi_instance_isolation.py"
      via: "required pytest command on push"
      pattern: "test_multi_instance_isolation.py"
    - from: ".github/workflows/ci.yml js job"
      to: "scripts/ci/verify_bundle_contract.py"
      via: "post-build artifact assertion"
      pattern: "verify_bundle_contract.py"
    - from: ".github/workflows/release.yml"
      to: "scripts/ci/check_tag_version.py"
      via: "fail-fast semver validation before publish"
      pattern: "v*.*.*"
---

<objective>
Automate CI/CD so quality and publish behavior are enforced by workflows instead of manual steps.

Purpose: satisfy CI-01, CI-02, CI-03, and PKG-03 with semver-safe, gated automation that includes multi-instance correctness checks before release.
Output: `ci.yml`, `release.yml`, and helper scripts for tag/version and bundle-contract validation.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-packaging-docs-ci-cd/09-RESEARCH.md
@.planning/phases/09-packaging-docs-ci-cd/09-VALIDATION.md
@.planning/phases/09-packaging-docs-ci-cd/09-01-PLAN.md
@.planning/phases/09-packaging-docs-ci-cd/09-02-PLAN.md

Non-negotiable CI gates in this plan:
- multi-instance isolation tests must run on push
- JS bundle contract assertions must run on push
- release publish must be tag-gated and semver-validated
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add push/PR CI workflow with Python, JS, package, and isolation gates</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Create `.github/workflows/ci.yml` with at least these jobs:
    - `python-tests`: install deps and run pytest gates, including:
      - `tests/test_packaging_smoke.py`
      - `tests/test_docs_examples_contract.py`
      - `tests/test_multi_instance_isolation.py`
      - `tests/test_dash_runtime_callbacks.py`
      - `tests/test_session_request_gate.py`
      - `tests/test_runtime_service.py`
    - `js-build`: run `npm ci` and `npm run build` in `dash_tanstack_pivot/`, then run `scripts/ci/verify_bundle_contract.py`
    - `package-smoke`: build distribution and run import smoke checks

    Configure workflow on push and pull_request events.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('.github/workflows/ci.yml').read_text(encoding='utf-8'); req=['push','pull_request','pytest','npm run build','test_multi_instance_isolation.py','verify_bundle_contract.py']; miss=[x for x in req if x not in t]; print('ci-yaml-ok' if not miss else 'missing:'+','.join(miss)); raise SystemExit(0 if not miss else 1)"</automated>
  </verify>
  <done>Push/PR CI workflow exists with explicit test/build/isolation gates.</done>
</task>

<task type="auto">
  <name>Task 2: Add release workflow for semantic tags with publish gating</name>
  <files>.github/workflows/release.yml, scripts/ci/check_tag_version.py</files>
  <action>
    Create `.github/workflows/release.yml` triggered by semantic tags (`v*.*.*`).
    Add release steps:
    1) checkout and setup Python
    2) run `scripts/ci/check_tag_version.py` to enforce tag/package version parity
    3) build artifacts (`python -m build dash_tanstack_pivot`)
    4) metadata validation (`twine check`)
    5) publish to PyPI using `pypa/gh-action-pypi-publish@release/v1`

    Ensure workflow depends on required quality gates or repeats critical smoke steps before publish.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('.github/workflows/release.yml').read_text(encoding='utf-8').lower(); req=['tags','v*.*.*','python -m build','twine check','gh-action-pypi-publish','check_tag_version.py']; miss=[x for x in req if x not in t]; print('release-yaml-ok' if not miss else 'missing:'+','.join(miss)); raise SystemExit(0 if not miss else 1)"</automated>
  </verify>
  <done>Release workflow is semver-tag gated and cannot publish without passing version/artifact checks.</done>
</task>

<task type="auto">
  <name>Task 3: Add CI helper scripts and run local dry-run verification commands</name>
  <files>scripts/ci/check_tag_version.py, scripts/ci/verify_bundle_contract.py</files>
  <action>
    Implement helper scripts:
    - `check_tag_version.py`: parse git tag/version and package metadata version; fail on mismatch
    - `verify_bundle_contract.py`: assert expected minified bundle files exist and no required file is missing

    Run dry-run local commands to validate script behavior and gate integration:
    - Python isolation test gate
    - JS build + bundle verification
    - package build smoke
  </action>
  <verify>
    <automated>python scripts/ci/check_tag_version.py --allow-no-tag && cd dash_tanstack_pivot && npm.cmd run build && cd .. && python scripts/ci/verify_bundle_contract.py && python -m pytest tests/test_multi_instance_isolation.py tests/test_dash_runtime_callbacks.py tests/test_session_request_gate.py tests/test_runtime_service.py -q</automated>
  </verify>
  <done>CI helper scripts are executable locally and enforce the same gates that workflows rely on.</done>
</task>

</tasks>

<verification>
1. `python -c "from pathlib import Path; t=Path('.github/workflows/ci.yml').read_text(encoding='utf-8'); req=['push','pull_request','pytest','npm run build','test_multi_instance_isolation.py']; miss=[x for x in req if x not in t]; raise SystemExit(0 if not miss else 1)"`
2. `python -c "from pathlib import Path; t=Path('.github/workflows/release.yml').read_text(encoding='utf-8').lower(); req=['tags','v*.*.*','gh-action-pypi-publish','twine check']; miss=[x for x in req if x not in t]; raise SystemExit(0 if not miss else 1)"`
3. `python scripts/ci/check_tag_version.py --allow-no-tag`
4. `cd dash_tanstack_pivot && npm.cmd run build && cd .. && python scripts/ci/verify_bundle_contract.py`
5. `python -m pytest tests/test_multi_instance_isolation.py tests/test_dash_runtime_callbacks.py tests/test_session_request_gate.py tests/test_runtime_service.py -q`
</verification>

<requirement_coverage>
| Requirement | Covered By | Evidence |
|-------------|------------|----------|
| CI-01 | Task 1 | push/PR Python pytest gates in ci workflow |
| CI-02 | Task 1, Task 3 | JS build job + bundle contract verification |
| CI-03 | Task 2 | tag-triggered publish workflow with PyPI action |
| PKG-03 | Task 2, Task 3 | semantic tag/version checks + gated publish path |
</requirement_coverage>

<multi_instance_coverage>
| Required Scope | Covered By | Evidence |
|----------------|------------|----------|
| Unique per-instance identity (`id`, `session_id`, `client_instance`) | Task 1 | required isolation tests on every push |
| Table-scoped requests per instance | Task 1, Task 3 | isolation test gate included in CI and local dry-run |
| Filter/sort isolation across instances | Task 1 | interleaved isolation tests required in CI |
| Concurrency correctness under interleaved requests | Task 1, Task 3 | dedicated pytest gate before release |
| Docs/examples proving 2+ instances in one app | Task 1 | docs/examples contract tests run in CI |
</multi_instance_coverage>

<success_criteria>
1. Push/PR workflow runs Python tests, JS build, package smoke, and multi-instance isolation gates.
2. Release workflow triggers only on semantic tags and enforces tag/version parity.
3. PyPI publish step is automated and occurs only after quality checks.
4. Multi-instance correctness remains a hard gate in CI and release paths.
</success_criteria>

<output>
After completion, create `.planning/phases/09-packaging-docs-ci-cd/09-03-SUMMARY.md`.
</output>

