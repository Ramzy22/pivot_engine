---
phase: 09-packaging-docs-ci-cd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dash_tanstack_pivot/pyproject.toml
  - dash_tanstack_pivot/setup.py
  - dash_tanstack_pivot/package.json
  - dash_tanstack_pivot/MANIFEST.in
  - dash_tanstack_pivot/dash_tanstack_pivot/__init__.py
  - tests/test_packaging_smoke.py
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-04
  - PKG-05

must_haves:
  truths:
    - "A canonical Python distribution named dash-tanstack-pivot is buildable from dash_tanstack_pivot/"
    - "Base install and redis extra metadata are declared and validated for package consumers"
    - "Import smoke succeeds after install with no missing dependency errors at import time"
    - "npm build emits the single minified bundle path expected by Python package metadata"
    - "Packaging verification keeps multi-instance runtime tests as a release gate"
  artifacts:
    - path: "dash_tanstack_pivot/pyproject.toml"
      provides: "Canonical package metadata including dependencies and optional redis extra"
      contains: "name = \"dash-tanstack-pivot\""
    - path: "dash_tanstack_pivot/setup.py"
      provides: "Compatibility wrapper aligned to pyproject metadata"
      contains: "setuptools"
    - path: "dash_tanstack_pivot/package.json"
      provides: "Valid build/publish script contract with no missing hook script"
      contains: "build"
    - path: "tests/test_packaging_smoke.py"
      provides: "Automated package and import smoke verification entry points"
      exports: ["test_import_dash_tanstack_pivot", "test_bundle_artifact_contract"]
  key_links:
    - from: "dash_tanstack_pivot/pyproject.toml"
      to: "pip install and import smoke commands"
      via: "project.dependencies + optional-dependencies.redis"
      pattern: "optional-dependencies"
    - from: "dash_tanstack_pivot/MANIFEST.in"
      to: "npm build output files"
      via: "declared static asset list"
      pattern: "dash_tanstack_pivot.min.js"
    - from: "tests/test_packaging_smoke.py"
      to: "PKG-01/02/04/05 acceptance checks"
      via: "automated smoke assertions"
      pattern: "test_"
---

<objective>
Establish a single, executable packaging baseline for `dash-tanstack-pivot` before docs and CI work begins.

Purpose: satisfy installation and import requirements early (PKG-01, PKG-02, PKG-04, PKG-05) and remove known packaging drift risk before later plans rely on these contracts.
Output: canonical package metadata, fixed build/publish script contract, and packaging smoke tests with deterministic verify commands.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-packaging-docs-ci-cd/09-RESEARCH.md
@.planning/phases/09-packaging-docs-ci-cd/09-VALIDATION.md

Known risks to close in this plan:
- Packaging metadata is currently fragmented and must be canonicalized.
- package.json prepublish hook references a missing validator script.
- MANIFEST/build artifact contract can drift unless tested.
- Multi-instance runtime behavior must remain guarded as packaging changes land.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize Python packaging metadata and redis extra</name>
  <files>dash_tanstack_pivot/pyproject.toml, dash_tanstack_pivot/setup.py, dash_tanstack_pivot/dash_tanstack_pivot/__init__.py</files>
  <action>
    Create/standardize `dash_tanstack_pivot/pyproject.toml` as the canonical metadata source with:
    - package name `dash-tanstack-pivot`
    - semantic version field
    - base dependencies needed for clean import path
    - `optional-dependencies.redis` for `pip install dash-tanstack-pivot[redis]`

    Align `dash_tanstack_pivot/setup.py` as a minimal compatibility shim (no diverging dependency/version definitions).
    Ensure `dash_tanstack_pivot/dash_tanstack_pivot/__init__.py` exposes package version/import surface consistent with metadata.
  </action>
  <verify>
    <automated>python -c "import pathlib,tomllib; p=pathlib.Path('dash_tanstack_pivot/pyproject.toml'); d=tomllib.loads(p.read_text(encoding='utf-8')); assert d['project']['name']=='dash-tanstack-pivot'; assert 'redis' in d['project']['optional-dependencies']; print('pkg-metadata-ok')"</automated>
  </verify>
  <done>Canonical package metadata exists and declares redis extra without conflicting duplicate metadata definitions.</done>
</task>

<task type="auto">
  <name>Task 2: Repair JS bundle/publish contract and manifest assertions</name>
  <files>dash_tanstack_pivot/package.json, dash_tanstack_pivot/MANIFEST.in, tests/test_packaging_smoke.py</files>
  <action>
    Remove or replace broken `prepublishOnly` references to missing scripts so publish/build commands are executable.
    Make `MANIFEST.in` match real build artifacts (including source map policy: either generated and present, or no longer declared).
    Add `tests/test_packaging_smoke.py` checks for bundle contract:
    - one minified bundle file expected by package metadata
    - no missing required artifact referenced by manifest/package init
  </action>
  <verify>
    <automated>cd dash_tanstack_pivot; npm.cmd run build; cd ..; python -m pytest tests/test_packaging_smoke.py -k bundle -q</automated>
  </verify>
  <done>`npm run build` is clean and automated bundle assertions pass against package manifest expectations.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic install/import smoke for base and redis extra</name>
  <files>tests/test_packaging_smoke.py</files>
  <action>
    Extend `tests/test_packaging_smoke.py` with smoke entry points and command coverage for:
    - build artifact creation (`python -m build dash_tanstack_pivot`)
    - base install path and import (`dash_tanstack_pivot`)
    - redis extra install path
    - runtime regression gate for multi-instance contract tests after packaging changes

    Keep commands non-interactive and CI-safe.
  </action>
  <verify>
    <automated>python -m build dash_tanstack_pivot && python -m pip install --force-reinstall .\dash_tanstack_pivot && python -m pip install --force-reinstall ".\dash_tanstack_pivot[redis]" && python -c "import dash_tanstack_pivot; print('import-ok')" && python -m pytest tests/test_packaging_smoke.py tests/test_dash_runtime_callbacks.py tests/test_session_request_gate.py -q</automated>
  </verify>
  <done>Base install, redis extra path, import smoke, and multi-instance regression gates all pass in one deterministic command chain.</done>
</task>

</tasks>

<verification>
1. `python -c "import pathlib,tomllib; d=tomllib.loads(pathlib.Path('dash_tanstack_pivot/pyproject.toml').read_text(encoding='utf-8')); assert d['project']['name']=='dash-tanstack-pivot'; assert 'redis' in d['project']['optional-dependencies']"`
2. `cd dash_tanstack_pivot && npm.cmd run build`
3. `python -m build dash_tanstack_pivot`
4. `python -m pip install --force-reinstall .\dash_tanstack_pivot && python -m pip install --force-reinstall ".\dash_tanstack_pivot[redis]" && python -c "import dash_tanstack_pivot"`
5. `python -m pytest tests/test_packaging_smoke.py tests/test_dash_runtime_callbacks.py tests/test_session_request_gate.py -q`
</verification>

<requirement_coverage>
| Requirement | Covered By | Evidence |
|-------------|------------|----------|
| PKG-01 | Task 1, Task 3 | Canonical package metadata + base install/import smoke |
| PKG-02 | Task 1, Task 3 | `optional-dependencies.redis` + redis install smoke |
| PKG-04 | Task 1, Task 3 | clean import smoke after install |
| PKG-05 | Task 2 | npm build + bundle artifact assertions |
</requirement_coverage>

<success_criteria>
1. Package metadata has one canonical source for `dash-tanstack-pivot` and redis extra.
2. Build/publish scripts do not reference missing helper scripts.
3. JS bundle and manifest contracts are automated and passing.
4. Base install, redis install path, and import smoke all pass.
5. Packaging changes do not regress multi-instance runtime tests.
</success_criteria>

<output>
After completion, create `.planning/phases/09-packaging-docs-ci-cd/09-01-SUMMARY.md`.
</output>
