---
phase: 09-packaging-docs-ci-cd
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - README.md
  - CHANGELOG.md
  - examples/example_dash_basic.py
  - examples/example_dash_hierarchical.py
  - examples/example_dash_sql_multi_instance.py
  - tests/test_docs_examples_contract.py
  - tests/test_multi_instance_isolation.py
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04

must_haves:
  truths:
    - "README includes a working 10-line minimal example for dash-tanstack-pivot installation and usage"
    - "README documents Python props with type, default, and description in a stable table format"
    - "README treats multi-instance safety as a first-class contract with explicit identity and isolation guarantees"
    - "At least three runnable Dash examples exist: basic, hierarchical, and SQL-connected"
    - "At least one example app runs two pivot instances in the same layout with unique ids and table-scoped requests"
    - "Filter/sort state and concurrent interleaved requests are proven isolated across instances by automated tests"
    - "CHANGELOG.md exists with initial semantic-versioned release scaffold"
  artifacts:
    - path: "README.md"
      provides: "Install, quickstart, props reference, and explicit multi-instance contract documentation"
      contains: "session_id"
    - path: "examples/example_dash_sql_multi_instance.py"
      provides: "Two-instance example with distinct id/session_id/client_instance/table wiring"
      contains: "DashTanstackPivot("
    - path: "tests/test_multi_instance_isolation.py"
      provides: "Interleaved request isolation checks for filter/sort and concurrency behavior"
      exports: ["test_table_scoped_isolation", "test_interleaved_requests_do_not_cross_instance_state"]
    - path: "CHANGELOG.md"
      provides: "Initialized release history format with initial entries"
      contains: "## [Unreleased]"
  key_links:
    - from: "README multi-instance contract section"
      to: "examples/example_dash_sql_multi_instance.py"
      via: "documented code path for two-instance wiring"
      pattern: "client_instance"
    - from: "examples/example_dash_sql_multi_instance.py"
      to: "tests/test_multi_instance_isolation.py"
      via: "same identity keys and table-scoped request assumptions"
      pattern: "table="
    - from: "tests/test_multi_instance_isolation.py"
      to: "runtime callback/session gate behavior"
      via: "interleaved request assertions"
      pattern: "session_id"
---

<objective>
Deliver release-grade documentation and runnable examples, with multi-instance safety documented and proven as a core user-facing behavior.

Purpose: satisfy DOC-01 through DOC-04 while making multi-instance isolation (identity, table scope, filter/sort separation, and concurrency correctness) explicit and test-backed.
Output: updated README, initialized CHANGELOG, three runnable examples, and isolation-focused automated tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-packaging-docs-ci-cd/09-RESEARCH.md
@.planning/phases/09-packaging-docs-ci-cd/09-VALIDATION.md
@.planning/phases/09-packaging-docs-ci-cd/09-01-PLAN.md

Critical scope in this plan:
- docs must describe per-instance identity keys (`id`, `session_id`, `client_instance`)
- examples must show table-scoped requests and 2+ instances in one app
- tests must prove filter/sort isolation and interleaved concurrency correctness
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README with quickstart, full props table, and explicit multi-instance contract</name>
  <files>README.md</files>
  <action>
    Replace backend-focused README copy with package-consumer docs for `dash-tanstack-pivot`.
    Include:
    - install commands (`pip install dash-tanstack-pivot` and redis extra)
    - 10-line minimal Dash example (DOC-01)
    - full Python props table: name, type, default, description (DOC-02)
    - dedicated "Multi-instance Safety Contract" section that explicitly states:
      1) unique `id`, `session_id`, `client_instance` per instance
      2) all requests remain table-scoped by `table`
      3) filter/sort isolation across instances
      4) interleaved response concurrency behavior
      5) link to two-instance runnable example
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('README.md').read_text(encoding='utf-8'); req=['pip install dash-tanstack-pivot','session_id','client_instance','table-scoped','10-line','| Prop | Type | Default | Description |']; miss=[x for x in req if x not in t]; print('readme-contract-ok' if not miss else 'missing:'+','.join(miss)); raise SystemExit(0 if not miss else 1)"</automated>
  </verify>
  <done>README is consumer-facing and contains explicit, test-aligned multi-instance contract language.</done>
</task>

<task type="auto">
  <name>Task 2: Add three runnable Dash examples including one two-instance SQL app</name>
  <files>examples/example_dash_basic.py, examples/example_dash_hierarchical.py, examples/example_dash_sql_multi_instance.py, tests/test_docs_examples_contract.py</files>
  <action>
    Add/update three example apps:
    - `example_dash_basic.py`: single-instance DataFrame quickstart
    - `example_dash_hierarchical.py`: hierarchical configuration example
    - `example_dash_sql_multi_instance.py`: SQL-connected two-instance app proving isolation in one layout

    In the two-instance example, require distinct values for:
    - component `id`
    - `session_id`
    - `client_instance`
    - `table`

    Add `tests/test_docs_examples_contract.py` to assert:
    - all three example files exist and import
    - two-instance example declares at least two pivot components with distinct ids/tables
    - docs-referenced examples match real paths
  </action>
  <verify>
    <automated>python -m pytest tests/test_docs_examples_contract.py -q</automated>
  </verify>
  <done>Three runnable examples are in place and contract-tested, including one two-instance SQL example.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize changelog and add interleaved multi-instance isolation tests</name>
  <files>CHANGELOG.md, tests/test_multi_instance_isolation.py</files>
  <action>
    Create `CHANGELOG.md` with semantic-versioned structure and initial entries (DOC-04).
    Add `tests/test_multi_instance_isolation.py` with explicit checks for:
    - table-scoped request isolation across two instances
    - filter/sort state isolation under interleaved requests
    - concurrency correctness (stale/out-of-order responses remain isolated per instance identity)
    - identity contract coverage (`id`, `session_id`, `client_instance`)

    Keep tests deterministic and executable in local/CI environments.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; c=Path('CHANGELOG.md'); assert c.exists() and '## [Unreleased]' in c.read_text(encoding='utf-8'); print('changelog-ok')" && python -m pytest tests/test_multi_instance_isolation.py tests/test_dash_runtime_callbacks.py tests/test_session_request_gate.py tests/test_runtime_service.py -q</automated>
  </verify>
  <done>CHANGELOG is initialized and multi-instance isolation is proven by focused automated tests.</done>
</task>

</tasks>

<verification>
1. `python -c "from pathlib import Path; t=Path('README.md').read_text(encoding='utf-8'); req=['pip install dash-tanstack-pivot','session_id','client_instance','table-scoped']; miss=[x for x in req if x not in t]; raise SystemExit(0 if not miss else 1)"`
2. `python -m pytest tests/test_docs_examples_contract.py -q`
3. `python -m pytest tests/test_multi_instance_isolation.py tests/test_dash_runtime_callbacks.py tests/test_session_request_gate.py tests/test_runtime_service.py -q`
4. `python -c "from pathlib import Path; p=Path('CHANGELOG.md'); raise SystemExit(0 if p.exists() and '## [Unreleased]' in p.read_text(encoding='utf-8') else 1)"`
</verification>

<requirement_coverage>
| Requirement | Covered By | Evidence |
|-------------|------------|----------|
| DOC-01 | Task 1 | README includes working 10-line minimal example |
| DOC-02 | Task 1 | README props table with type/default/description |
| DOC-03 | Task 2, Task 3 | three runnable examples + contract tests |
| DOC-04 | Task 3 | initialized CHANGELOG with release scaffold |
</requirement_coverage>

<multi_instance_coverage>
| Required Scope | Covered By | Evidence |
|----------------|------------|----------|
| Unique per-instance identity (`id`, `session_id`, `client_instance`) | Task 1, Task 2, Task 3 | README contract + two-instance example + isolation tests |
| Table-scoped requests per instance | Task 2, Task 3 | SQL multi-instance example + table isolation test |
| Filter/sort isolation across instances | Task 3 | interleaved isolation pytest assertions |
| Concurrency correctness under interleaved requests | Task 3 | stale/out-of-order request tests per instance identity |
| Docs/examples proving 2+ instances in one app | Task 1, Task 2 | README link + `example_dash_sql_multi_instance.py` |
</multi_instance_coverage>

<success_criteria>
1. README provides install, 10-line quickstart, full prop docs, and explicit multi-instance contract.
2. Three documented examples run/import, including a two-instance SQL-connected app.
3. CHANGELOG.md exists with an initial semantic-release scaffold.
4. Multi-instance identity, table scope, filter/sort isolation, and interleaved concurrency are test-covered and green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-packaging-docs-ci-cd/09-02-SUMMARY.md`.
</output>

