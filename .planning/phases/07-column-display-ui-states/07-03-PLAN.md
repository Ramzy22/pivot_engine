---
phase: 07-column-display-ui-states
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js
  - dash_tanstack_pivot/src/lib/utils/styles.js
  - .planning/phases/07-column-display-ui-states/07-UI-STATE-CHECKLIST.md
autonomous: false
requirements: [UI-05, UI-06]

must_haves:
  truths:
    - "Combined column states (pinned + sorted + resized) render without clipping, overlap, or conflicting emphasis"
    - "Default column widths, row heights, and header heights stay visually consistent across density modes and refreshes"
    - "Density/default sizing choices are applied consistently across header rows, body rows, and floating filter rows"
    - "A repeatable manual verification checklist exists for all UI-01 through UI-06 scenarios"
  artifacts:
    - path: "dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js"
      provides: "Centralized density/size constants and combined-state style precedence wiring"
      contains: "rowHeights"
    - path: "dash_tanstack_pivot/src/lib/utils/styles.js"
      provides: "Shared visual tokens for default dimensions and state emphasis"
      contains: "headerCell"
    - path: ".planning/phases/07-column-display-ui-states/07-UI-STATE-CHECKLIST.md"
      provides: "Manual UAT matrix for pin/sort/hide/resize/default-density and combined-state checks"
  key_links:
    - from: "dimension token constants"
      to: "column definitions, auto-size bounds, and row/header height calculations"
      via: "replace hard-coded width/height literals with named defaults"
      pattern: "size:"
    - from: "combined-state style composition in header/body render paths"
      to: "manual checklist scenarios"
      via: "pinned+sorted+resized verification matrix"
      pattern: "pinned|sorted|resize"
---

<objective>
Complete Phase 7 by hardening combined-state rendering and consolidating UI defaults so appearance remains stable across data density and interaction combinations.

Purpose: UI-05 and UI-06 are integration-level quality requirements. After pin/sort/resize/visibility paths are corrected, this plan removes literal-driven drift and validates that combined states remain coherent.

Output: Centralized default dimensions, combined-state style precedence, and a formal Phase 7 manual checklist with blocking human verification.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-column-display-ui-states/07-RESEARCH.md
@.planning/phases/07-column-display-ui-states/07-VALIDATION.md
@.planning/phases/07-column-display-ui-states/07-01-SUMMARY.md
@.planning/phases/07-column-display-ui-states/07-02-SUMMARY.md

Current implementation has dimension literals spread across:
- row-height arrays and spacing labels
- column `size` defaults in column builders
- auto-size clamp bounds (min/max width)
- header/body/floating-filter row height calculations

This plan unifies those values and verifies combined-state rendering at the UI level.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize default dimensions and combined-state style precedence</name>
  <files>dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js, dash_tanstack_pivot/src/lib/utils/styles.js</files>
  <action>
    Normalize UI defaults and combined-state composition:

    1. Introduce named constants for default dimensions:
       - density presets (compact/normal/loose row heights)
       - default column widths for hierarchy, dimension, measure, subtotal columns
       - auto-size min/max bounds

    2. Replace scattered numeric literals with these constants in:
       - row-height derivation
       - column builder `size` assignments
       - auto-size clamp logic
       - header/floating-filter/body row height calculations

    3. Add a deterministic style precedence helper (or equivalent inline composition) so combined states apply in a fixed order:
       `base -> sorted-active -> pinned-boundary -> selected/focus overlay`
       Apply this consistently to both header cells and body cells where states overlap.
  </action>
  <verify>
    <automated>rg -n "rowHeights|spacingLabels|size: 250|size: 150|size: 130|size: 60|maxWidth = Math|minWidth" dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js</automated>
    <automated>python -c "from pathlib import Path; t=Path('dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js').read_text(encoding='utf-8'); req=['rowHeights','spacingLabels']; m=[x for x in req if x not in t]; print('dimension-smoke-ok' if not m else 'missing:'+','.join(m)); raise SystemExit(0 if not m else 1)"</automated>
  </verify>
  <done>
    Default dimensions come from centralized tokens and combined-state style composition is deterministic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 7 UI verification checklist document</name>
  <files>.planning/phases/07-column-display-ui-states/07-UI-STATE-CHECKLIST.md</files>
  <action>
    Create `.planning/phases/07-column-display-ui-states/07-UI-STATE-CHECKLIST.md` with explicit pass/fail steps for:

    - UI-01: pinned left and right separators during horizontal scroll
    - UI-02: asc/desc indicators + active sorted emphasis
    - UI-03: hide/show columns and persistence through filter/data refresh
    - UI-04: hover/focus resize handles + resize persistence through refresh
    - UI-05: combined pinned + sorted + resized columns (left and right) with no visual conflicts
    - UI-06: compact/normal/loose density balance and consistent header/body spacing

    Include a small table with: scenario, expected result, status, notes.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('.planning/phases/07-column-display-ui-states/07-UI-STATE-CHECKLIST.md'); print('checklist-created' if p.exists() else 'missing'); raise SystemExit(0 if p.exists() else 1)"</automated>
  </verify>
  <done>
    A reusable checklist exists to drive final manual sign-off for all Phase 7 UI states.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification gate for combined-state UI quality</name>
  <files>.planning/phases/07-column-display-ui-states/07-UI-STATE-CHECKLIST.md, dash_presentation/app.py</files>
  <action>
    Execute the full Phase 7 checklist, record scenario outcomes (pass/fail + notes), and confirm no visual conflicts remain for combined pinned/sorted/resized states across density modes.
  </action>
  <what-built>
    - Centralized default dimension tokens and combined-state style precedence.
    - Manual checklist document for UI-01 through UI-06 verification.
  </what-built>
  <how-to-verify>
    1. Start app: `cd dash_presentation && python app.py`
    2. Open the pivot UI and execute every scenario in:
       `.planning/phases/07-column-display-ui-states/07-UI-STATE-CHECKLIST.md`
    3. For each scenario, mark pass/fail and add notes/screenshots as needed.
    4. Confirm specifically that a column can be pinned + sorted + resized at once with no clipped icons, doubled shadows, or header/body misalignment.
    5. Confirm compact/normal/loose spacing modes remain visually balanced with grouped headers and floating filters enabled.
  </how-to-verify>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('.planning/phases/07-column-display-ui-states/07-UI-STATE-CHECKLIST.md').read_text(encoding='utf-8'); req=['UI-01','UI-02','UI-03','UI-04','UI-05','UI-06']; m=[x for x in req if x not in t]; print('checklist-ready' if not m else 'missing:'+','.join(m)); raise SystemExit(0 if not m else 1)"</automated>
    <manual>Complete every scenario in `.planning/phases/07-column-display-ui-states/07-UI-STATE-CHECKLIST.md` and record pass/fail plus notes.</manual>
    <manual>Validate the combined-state path (pinned + sorted + resized) in both left and right pinned areas.</manual>
  </verify>
  <done>
    Human reviewer signs off that all checklist scenarios pass, or returns failing scenario IDs for rework.
  </done>
  <resume-signal>Type "approved" if all checklist scenarios pass, or provide failing scenario IDs and observed behavior.</resume-signal>
</task>

</tasks>

<verification>
1. `cd dash_tanstack_pivot && npm.cmd run build` exits 0.
2. `python -m pytest tests/test_frontend_contract.py tests/test_frontend_filters.py -q` exits 0.
3. Checklist file exists and is populated with executable scenarios.
4. Human verification checkpoint passes all UI-01 through UI-06 checks.
</verification>

<success_criteria>
1. UI-05: Combined pinned/sorted/resized states render without visual conflict.
2. UI-06: Default widths/heights are balanced and consistent across density modes.
3. Phase 7 has a repeatable manual sign-off artifact for regression checks.
4. No regressions in existing test suites and JS build.
</success_criteria>

<output>
After completion, create `.planning/phases/07-column-display-ui-states/07-03-SUMMARY.md`
</output>
