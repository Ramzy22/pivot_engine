---
phase: 07-column-display-ui-states
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - dash_tanstack_pivot/src/lib/hooks/useStickyStyles.js
  - dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js
  - dash_tanstack_pivot/src/lib/utils/styles.js
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "Pinned columns stay fixed during horizontal scroll and display an explicit boundary separator/shadow"
    - "Each pinned edge renders a single clear boundary separator with no doubled shadows or flicker"
    - "Sorted headers show ascending/descending indicators and a clear active-sorted visual treatment"
    - "Sorted visual styling remains legible when the sorted column is also pinned"
  artifacts:
    - path: "dash_tanstack_pivot/src/lib/hooks/useStickyStyles.js"
      provides: "Sticky geometry and boundary style logic for pinned headers/cells"
      contains: "getIsLastColumn('left')"
    - path: "dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js"
      provides: "Sorted-active header styling wired into renderHeaderCell"
      contains: "getIsSorted"
    - path: "dash_tanstack_pivot/src/lib/utils/styles.js"
      provides: "Theme tokens for sorted and pinned state emphasis"
      contains: "headerBg"
  key_links:
    - from: "useStickyStyles.getHeaderStickyStyle/getStickyStyle"
      to: "renderHeaderCell/renderCell"
      via: "single source of pinned offsets and boundary separators"
      pattern: "boxShadow"
    - from: "renderHeaderCell sorted state"
      to: "header style contract"
      via: "getIsSorted + icon + active style token"
      pattern: "SortAsc|SortDesc"
---

<objective>
Implement the visual contract for pinning and sorting so UI state is immediately understandable in dense tables and under combined interactions.

Purpose: UI-01 and UI-02 are primarily rendering and affordance requirements. The code already has pin/sort primitives, but style precedence and boundary logic are spread between the hook and component. This plan consolidates those rules and hardens clarity.

Output: useStickyStyles, DashTanstackPivot.react.js, and style tokens updated for consistent pinned boundaries and sorted-active header visuals.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-column-display-ui-states/07-RESEARCH.md
@.planning/phases/07-column-display-ui-states/07-VALIDATION.md

Current reality from code:
- `useStickyStyles` computes offsets and applies sticky positioning, but body-level boundary shadows are also manually applied in `renderCell`, creating overlap risk.
- Sorting icons are present in headers, but there is no explicit sorted-active background/border token.
- UI requirements need clear visual distinction when sorting and pinning coexist.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate pinned geometry and separator rendering into the sticky-style hook</name>
  <files>dash_tanstack_pivot/src/lib/hooks/useStickyStyles.js, dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js</files>
  <action>
    Normalize pinning visuals and eliminate duplicated boundary logic:

    1. Update `useStickyStyles` so pinned offsets and edge separators are produced in one place for both headers and body cells.
    2. Use deterministic left/right boundary detection (`last pinned left`, `first pinned right`) to apply separator shadows/borders.
    3. Keep pinned z-index layering explicit: header pinned above body pinned, with no overlap artifacts.
    4. Remove duplicate manual boundary-shadow mutations from `renderCell` that conflict with hook output.

    Constraint: keep current behavior for non-pinned columns unchanged.
  </action>
  <verify>
    <automated>rg -n "getStickyStyle|getHeaderStickyStyle|getIsLastColumn|getIsFirstColumn|boxShadow" dash_tanstack_pivot/src/lib/hooks/useStickyStyles.js dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js</automated>
  </verify>
  <done>
    Pinned boundary rendering is consistent and originates from one source of truth, with no double-shadow conflicts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add explicit sorted-active header styling while preserving existing sort behavior</name>
  <files>dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js, dash_tanstack_pivot/src/lib/utils/styles.js</files>
  <action>
    Add a visible sorted-state contract on top of existing sort mechanics:

    1. Introduce sorted-state style tokens (subtle active background + accent border + text emphasis) in style/theme utilities.
    2. In `renderHeaderCell`, compute a `sortedActiveStyle` from `header.column.getIsSorted()` and merge it with existing header style.
    3. Keep current icon indicators (`SortAsc` / `SortDesc`) and multi-sort index badge.
    4. Ensure sorted style remains visible when column is pinned by applying sorted style before sticky overlay merge.
    5. Preserve keyboard sorting and aria-sort semantics.
  </action>
  <verify>
    <automated>rg -n "getIsSorted|SortAsc|SortDesc|aria-sort|sorted" dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js</automated>
    <automated>python -c "from pathlib import Path; t=Path('dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js').read_text(encoding='utf-8'); req=['getIsSorted','SortAsc','SortDesc','aria-sort']; m=[x for x in req if x not in t]; print('sort-smoke-ok' if not m else 'missing:'+','.join(m)); raise SystemExit(0 if not m else 1)"</automated>
  </verify>
  <done>
    Sorted headers are visually distinct beyond icon-only affordance, and existing sorting behavior remains intact.
  </done>
</task>

</tasks>

<verification>
1. `cd dash_tanstack_pivot && npm.cmd run build` exits 0.
2. `python -m pytest tests/test_frontend_contract.py -q -k "sorting or virtual_scroll_preserves_sort_and_grand_total"` exits 0.
3. Manual spot check:
   - Pin at least one left and one right column.
   - Scroll horizontally and confirm pinned columns stay fixed with clear boundary separators.
   - Sort a pinned column and confirm icon + active-sorted visual emphasis are both visible.
</verification>

<success_criteria>
1. UI-01: Pinned columns remain fixed on horizontal scroll with clear visual separators.
2. UI-02: Sorted columns display asc/desc indicators and active-sorted emphasis.
3. Pinned + sorted coexistence remains visually clear without artifacts.
4. Existing sorting regressions are not introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-column-display-ui-states/07-02-SUMMARY.md`
</output>
