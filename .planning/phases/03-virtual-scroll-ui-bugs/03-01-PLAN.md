---
phase: 03-virtual-scroll-ui-bugs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_frontend_contract.py
  - tests/test_frontend_filters.py
  - test_expand_all_backend.py
  - pivot_engine/tests/test_hierarchical_managers.py
autonomous: true
requirements:
  - BUG-07
  - BUG-08
  - BUG-11
  - BUG-12

must_haves:
  truths:
    - "Phase 3 starts with explicit regressions for stale rows, blank rows, row duplication, and expand-collapse continuity"
    - "Hierarchy tests assert visible-row count and sibling stability, not only happy-path data presence"
    - "Virtual-scroll request sequences are exercised before implementation changes start"
  artifacts:
    - path: "tests/test_frontend_contract.py"
      provides: "Adapter-level regressions for virtual-scroll and expand-collapse continuity"
      contains: "virtual"
    - path: "test_expand_all_backend.py"
      provides: "Hierarchy-manager regressions for expand-all and sibling stability"
      contains: "expanded"
    - path: "pivot_engine/tests/test_hierarchical_managers.py"
      provides: "Manager-level pagination and visible-row-count regressions"
      contains: "paginated"
  key_links:
    - from: "tests/test_frontend_contract.py"
      to: "pivot_engine/pivot_engine/tanstack_adapter.py"
      via: "adapter request -> hierarchical/virtual-scroll response assertions"
      pattern: "handle_hierarchical_request|handle_virtual_scroll_request"
---

<objective>
Add the missing regression net for Phase 3 before changing hierarchy, cache, or UI behavior. The current suite proves only part of the backend contract and leaves the most visible failure modes underspecified.

Purpose: make Phase 3 implementation evidence-driven.
Output: explicit regressions for stale rows, visible-row continuity, expand/collapse stability, and virtual-scroll request sequencing.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-virtual-scroll-ui-bugs/03-RESEARCH.md
@.planning/phases/03-virtual-scroll-ui-bugs/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend adapter-level virtual-scroll regressions</name>
  <files>tests/test_frontend_contract.py, tests/test_frontend_filters.py</files>
  <action>
    Add request-sequence regressions that prove the virtual-scroll and hierarchical paths stay logically synchronized across state changes.

    Cover these sequences:
    - initial hierarchical load -> virtual-scroll request
    - scroll request -> sort/filter change -> next scroll request
    - expand -> collapse -> re-request while preserving stable sibling ordering
    - repeated requests that would expose stale cached blocks or blank rows

    Assert on returned rows, ordering, row count metadata, and uniqueness of returned row identifiers where possible. Prefer compact fixtures over large scenario duplication.
  </action>
  <verify>
    <automated>python -m pytest tests/test_frontend_contract.py tests/test_frontend_filters.py -q --tb=line</automated>
  </verify>
  <done>Adapter regressions now prove stale-row and continuity failures instead of only checking happy-path sorting.</done>
</task>

<task type="auto">
  <name>Task 2: Extend hierarchy-manager regressions</name>
  <files>test_expand_all_backend.py, pivot_engine/tests/test_hierarchical_managers.py</files>
  <action>
    Add manager-level tests that exercise visible-row counting and sibling stability under expansion changes.

    Requirements:
    - verify no duplicate siblings after expand/collapse
    - verify pagination/windowing remains consistent after visibility changes
    - verify expand-all and selective expansion produce consistent row totals

    Focus on deterministic assertions rather than print-based diagnostics.
  </action>
  <verify>
    <automated>python -m pytest test_expand_all_backend.py pivot_engine/tests/test_hierarchical_managers.py -q --tb=line</automated>
  </verify>
  <done>Hierarchy-manager regressions clearly expose any broken visible-row accounting or sibling corruption.</done>
</task>

</tasks>

<verification>
Run the Phase 3 quick suite subset:

`python -m pytest tests/test_frontend_contract.py tests/test_frontend_filters.py test_expand_all_backend.py pivot_engine/tests/test_hierarchical_managers.py pivot_engine/tests/test_scalable_pivot.py -q --tb=line`

Expected: the new tests are collected and provide a clear red/green signal for the targeted Phase 3 bugs.
</verification>

<success_criteria>
- Phase 3 has explicit regressions for BUG-07, BUG-08, BUG-11, and BUG-12 before implementation work starts
- New assertions target row continuity and visible-row accounting, not only field presence
- Existing Phase 2 contract coverage remains green
</success_criteria>

<output>
After completion, create `.planning/phases/03-virtual-scroll-ui-bugs/03-01-SUMMARY.md` following the summary template.
</output>
