---
phase: 03-virtual-scroll-ui-bugs
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - pivot_engine/pivot_engine/tanstack_adapter.py
  - pivot_engine/pivot_engine/hierarchical_scroll_manager.py
  - pivot_engine/pivot_engine/scalable_pivot_controller.py
  - tests/test_frontend_contract.py
  - test_expand_all_backend.py
  - pivot_engine/tests/test_hierarchical_managers.py
autonomous: true
requirements:
  - BUG-07
  - BUG-11
  - BUG-12

must_haves:
  truths:
    - "Expand and collapse operations return the correct children at the correct indentation"
    - "Visible-row counts and offsets stay consistent after hierarchy visibility changes"
    - "Backend row identity and ordering semantics are stable enough for frontend virtualization to consume safely"
  artifacts:
    - path: "pivot_engine/pivot_engine/tanstack_adapter.py"
      provides: "Stable hierarchical and virtual-scroll response shaping"
      contains: "expanded_paths"
    - path: "pivot_engine/pivot_engine/hierarchical_scroll_manager.py"
      provides: "Correct visible-row accounting and hierarchy paging"
      contains: "total_visible_rows"
  key_links:
    - from: "pivot_engine/pivot_engine/tanstack_adapter.py"
      to: "pivot_engine/pivot_engine/hierarchical_scroll_manager.py"
      via: "adapter hierarchy request -> visible-row manager -> response rows"
      pattern: "handle_hierarchical_request|get_visible_rows_hierarchical"
---

<objective>
Fix the backend hierarchy semantics that feed the virtual-scroll UI. By the end of this plan, expand/collapse and pagination should produce stable children, stable sibling ordering, and stable visible-row counts.

Purpose: satisfy the backend half of BUG-07, BUG-11, and BUG-12.
Output: corrected adapter and hierarchy-manager behavior with passing hierarchy regressions.
</objective>

<context>
@.planning/phases/03-virtual-scroll-ui-bugs/03-RESEARCH.md
@.planning/phases/03-virtual-scroll-ui-bugs/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix generic hierarchy expansion and row identity semantics</name>
  <files>pivot_engine/pivot_engine/tanstack_adapter.py, tests/test_frontend_contract.py, test_expand_all_backend.py</files>
  <action>
    Audit and fix the adapter's hierarchical response shaping so it does not rely on narrow hierarchy assumptions.

    Requirements:
    - child rows returned after expansion are the correct logical children
    - indentation/depth fields remain correct
    - row identity/path handling is stable across expand/collapse cycles
    - logic remains generic across different row-field combinations

    Do not patch the frontend first. The backend contract must become authoritative.
  </action>
  <verify>
    <automated>python -m pytest tests/test_frontend_contract.py test_expand_all_backend.py -q --tb=line</automated>
  </verify>
  <done>Expanded groups return the correct children and sibling ordering no longer corrupts after collapse/re-expand.</done>
</task>

<task type="auto">
  <name>Task 2: Fix visible-row accounting and hierarchical paging</name>
  <files>pivot_engine/pivot_engine/hierarchical_scroll_manager.py, pivot_engine/pivot_engine/scalable_pivot_controller.py, pivot_engine/tests/test_hierarchical_managers.py</files>
  <action>
    Fix hierarchy-manager paging and visible-row calculations so windowed results remain consistent after expansion changes.

    Important:
    - start/end row windows must map to the correct logical rows after expand/collapse
    - visible-row counts must reflect the active hierarchy state
    - no duplicate rows or skipped sibling rows are introduced by paging logic
  </action>
  <verify>
    <automated>python -m pytest pivot_engine/tests/test_hierarchical_managers.py pivot_engine/tests/test_scalable_pivot.py -q --tb=line</automated>
  </verify>
  <done>Hierarchy-manager pagination remains stable after visibility changes and scalable-controller virtual-scroll tests stay green.</done>
</task>

</tasks>

<verification>
Run the hierarchy-focused subset:

`python -m pytest tests/test_frontend_contract.py test_expand_all_backend.py pivot_engine/tests/test_hierarchical_managers.py pivot_engine/tests/test_scalable_pivot.py -q --tb=line`

Expected: hierarchy expansion and visible-row regressions pass without requiring frontend-only workarounds.
</verification>

<success_criteria>
- Expand/collapse returns correct children and indentation
- Sibling positions remain stable across visibility changes
- Hierarchical pagination and total visible row counts are internally consistent
- Backend contract is strong enough for frontend caching to rely on it
</success_criteria>

<output>
After completion, create `.planning/phases/03-virtual-scroll-ui-bugs/03-02-SUMMARY.md` following the summary template.
</output>
