---
phase: 03-virtual-scroll-ui-bugs
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js
  - dash_tanstack_pivot/src/lib/hooks/useServerSideRowModel.js
  - dash_tanstack_pivot/src/lib/hooks/useRowCache.js
  - tests/test_frontend_contract.py
  - tests/test_frontend_filters.py
autonomous: true
requirements:
  - BUG-07
  - BUG-08

must_haves:
  truths:
    - "Frontend block caching is scoped to request state strongly enough that stale blocks cannot leak across incompatible states"
    - "Virtual-scroll rendering does not leave blank rows or stale sibling rows after filter, sort, or expansion changes"
    - "Smooth scrolling behavior is preserved only where it does not violate correctness"
  artifacts:
    - path: "dash_tanstack_pivot/src/lib/hooks/useRowCache.js"
      provides: "Block-cache lifecycle and invalidation semantics"
      contains: "clearCache"
    - path: "dash_tanstack_pivot/src/lib/hooks/useServerSideRowModel.js"
      provides: "Viewport fetch logic and request-version handling"
      contains: "version"
  key_links:
    - from: "dash_tanstack_pivot/src/lib/hooks/useServerSideRowModel.js"
      to: "dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js"
      via: "viewport requests -> cached rows -> rendered row window"
      pattern: "viewport|virtualRows"
---

<objective>
Fix the frontend block-cache and row-window synchronization logic so virtual scrolling stays correct when request state changes.

Purpose: satisfy BUG-07 and BUG-08 on the React side.
Output: request-aware block caching with passing stale-row and blank-row regressions.
</objective>

<context>
@.planning/phases/03-virtual-scroll-ui-bugs/03-RESEARCH.md
@.planning/phases/03-virtual-scroll-ui-bugs/03-01-PLAN.md
@.planning/phases/03-virtual-scroll-ui-bugs/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make block-cache invalidation request-aware</name>
  <files>dash_tanstack_pivot/src/lib/hooks/useServerSideRowModel.js, dash_tanstack_pivot/src/lib/hooks/useRowCache.js</files>
  <action>
    Replace the current block-index-only invalidation assumptions with request-aware invalidation.

    Focus areas:
    - filter and sort changes
    - expansion changes
    - row-count or structural changes
    - stale request responses arriving out of order

    Use the smallest design that restores correctness. A request signature/version namespace is preferable to ad-hoc special cases.
  </action>
  <verify>
    <automated>python -m pytest tests/test_frontend_contract.py tests/test_frontend_filters.py -q --tb=line</automated>
  </verify>
  <done>Incompatible request states no longer share block cache and stale-row regressions pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix rendered-row synchronization in the grid component</name>
  <files>dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js, tests/test_frontend_contract.py</files>
  <action>
    Audit the virtual-row render path and remove correctness bugs caused by stale-while-revalidate assumptions.

    Requirements:
    - no blank rows while valid data exists for the requested window
    - no duplicate or stale rows after sort/filter/expand changes
    - row identity stays stable enough for the grid to render the correct logical row

    Do not broaden this into a component refactor. Limit the work to synchronization and correctness.
  </action>
  <verify>
    <automated>python -m pytest tests/test_frontend_contract.py -q --tb=line</automated>
  </verify>
  <done>The React render path stays synchronized with the active request state and no longer shows stale or blank rows for the covered regressions.</done>
</task>

</tasks>

<verification>
Run the virtual-scroll subset:

`python -m pytest tests/test_frontend_contract.py tests/test_frontend_filters.py -q --tb=line`

Expected: stale-row, blank-row, and request invalidation regressions pass.
</verification>

<success_criteria>
- Block cache invalidates on incompatible request-state changes
- Virtual-scroll windows no longer reuse stale rows across sort/filter/expand changes
- No broad refactor is needed to make the render path correct
</success_criteria>

<output>
After completion, create `.planning/phases/03-virtual-scroll-ui-bugs/03-03-SUMMARY.md` following the summary template.
</output>
