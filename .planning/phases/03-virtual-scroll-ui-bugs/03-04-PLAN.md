---
phase: 03-virtual-scroll-ui-bugs
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js
  - dash_tanstack_pivot/src/lib/hooks/useColumnVirtualizer.js
  - dash_tanstack_pivot/src/lib/hooks/useStickyStyles.js
  - dash_tanstack_pivot/src/lib/components/Table/ContextMenu.js
  - tests/test_frontend_contract.py
autonomous: true
requirements:
  - BUG-09
  - BUG-10
  - BUG-13

must_haves:
  truths:
    - "Header groups visually span the same width as their visible child columns"
    - "Pinned and virtualized header sections remain aligned with body cells"
    - "Context menus remain fully visible inside the viewport near any edge"
  artifacts:
    - path: "dash_tanstack_pivot/src/lib/hooks/useStickyStyles.js"
      provides: "Pinned header offset behavior for grouped headers"
      contains: "position: 'sticky'"
    - path: "dash_tanstack_pivot/src/lib/components/Table/ContextMenu.js"
      provides: "Viewport-safe menu placement"
      contains: "window.innerWidth"
  key_links:
    - from: "dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js"
      to: "dash_tanstack_pivot/src/lib/hooks/useStickyStyles.js"
      via: "header rendering -> pinned/group offset calculation"
      pattern: "header|getHeaderGroups"
---

<objective>
Fix the remaining visible UI defects in Phase 3: header geometry and context-menu placement. Then run the full verification pass for the phase.

Purpose: satisfy BUG-09, BUG-10, and BUG-13 and finish Phase 3 verification.
Output: aligned grouped headers, viewport-safe context menus, and a green full suite.
</objective>

<context>
@.planning/phases/03-virtual-scroll-ui-bugs/03-RESEARCH.md
@.planning/phases/03-virtual-scroll-ui-bugs/03-02-PLAN.md
@.planning/phases/03-virtual-scroll-ui-bugs/03-03-PLAN.md
@.planning/phases/03-virtual-scroll-ui-bugs/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix grouped-header alignment and span width behavior</name>
  <files>dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js, dash_tanstack_pivot/src/lib/hooks/useColumnVirtualizer.js, dash_tanstack_pivot/src/lib/hooks/useStickyStyles.js</files>
  <action>
    Audit the header rendering path and fix width/span mismatches for multi-level grouped headers.

    Focus areas:
    - grouped headers whose visible children are split across pinned and center sections
    - sticky offsets for pinned grouped headers
    - placeholder/group header sizing under horizontal virtualization

    Keep the implementation targeted to header geometry. Do not collapse this into a general UI rewrite.
  </action>
  <verify>
    <automated>python -m pytest tests/test_frontend_contract.py -q --tb=line</automated>
  </verify>
  <done>Header geometry changes are in place and manual grouped-header checks have a clear target to validate.</done>
</task>

<task type="auto">
  <name>Task 2: Fix context-menu viewport clamping and run full verification</name>
  <files>dash_tanstack_pivot/src/lib/components/Table/ContextMenu.js, dash_tanstack_pivot/src/lib/components/DashTanstackPivot.react.js</files>
  <action>
    Replace the current context-menu placement heuristic with actual viewport-safe clamping.

    Requirements:
    - clamp all four edges, not only right/bottom
    - avoid assumptions that break when action labels change menu height
    - preserve current menu actions and close behavior

    After the fix, run the Phase 3 full verification flow.
  </action>
  <verify>
    <automated>python -m pytest tests/ test_expand_all_backend.py test_filtering.py pivot_engine/tests/ pivot_engine/test_arrow_conversion.py pivot_engine/test_async_changes.py pivot_engine/test_cursor_simple.py pivot_engine/test_scalable_async_changes.py pivot_engine/test_totals_demo.py -v --tb=short</automated>
  </verify>
  <done>Context menus stay within the viewport and the full suite remains green after the UI fixes.</done>
</task>

</tasks>

<verification>
Run the quick suite, then the full suite:

`python -m pytest tests/test_frontend_contract.py tests/test_frontend_filters.py test_expand_all_backend.py pivot_engine/tests/test_hierarchical_managers.py pivot_engine/tests/test_scalable_pivot.py -q --tb=line`

`python -m pytest tests/ test_expand_all_backend.py test_filtering.py pivot_engine/tests/ pivot_engine/test_arrow_conversion.py pivot_engine/test_async_changes.py pivot_engine/test_cursor_simple.py pivot_engine/test_scalable_async_changes.py pivot_engine/test_totals_demo.py -v --tb=short`

Expected: Phase 3 regressions stay green and the existing baseline remains green after the final UI fixes.
</verification>

<success_criteria>
- Grouped headers visually align with their child columns
- Context menu stays fully within the viewport
- Full suite remains green
- Phase 3 requirements BUG-07 through BUG-13 are all represented by the full set of plans
</success_criteria>

<output>
After completion, create `.planning/phases/03-virtual-scroll-ui-bugs/03-04-SUMMARY.md` following the summary template.
</output>
