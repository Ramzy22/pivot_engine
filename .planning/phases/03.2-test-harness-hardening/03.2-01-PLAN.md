---
phase: 03.2-test-harness-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dash_presentation/app.py
  - tests/test_frontend_contract.py
autonomous: true
requirements:
  - QUAL-05

must_haves:
  truths:
    - "Collecting tests/test_frontend_contract.py does not trigger synthetic data generation or DuckDB bootstrap"
    - "dash_presentation.app remains importable and still exposes a valid Dash app object and layout"
    - "Heavy adapter initialization and initial data loading happen lazily through an explicit bootstrap path, not at module import time"
  artifacts:
    - path: "dash_presentation/app.py"
      provides: "Lazy adapter/bootstrap path that keeps app import-safe during test collection"
      contains: "get_adapter|ensure_adapter|load_initial_data"
    - path: "tests/test_frontend_contract.py"
      provides: "Frontend contract test that imports app inside a test or fixture instead of at module scope"
      exports:
        - test_dash_app_import_and_layout_valid
  key_links:
    - from: "tests/test_frontend_contract.py"
      to: "dash_presentation/app.py"
      via: "import-time app verification without module-scope backend initialization"
      pattern: "from dash_presentation.app import app|import dash_presentation.app"
---

<objective>
Remove the collection-time side effect described by QUAL-05. The frontend contract test module must collect cleanly, and the Dash app module must be import-safe even when pytest only inspects the file.

Purpose: make the test harness deterministic and cheap to collect without broad application refactoring.
Output: a lazy bootstrap path in `dash_presentation/app.py` and a collection-safe import/layout test in `tests/test_frontend_contract.py`.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.2-test-harness-hardening/03.2-RESEARCH.md
@.planning/phases/03.2-test-harness-hardening/03.2-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make dash_presentation.app import-safe via lazy adapter bootstrap</name>
  <files>dash_presentation/app.py</files>
  <action>
    Replace the current module-scope adapter creation and `load_initial_data()` call with a narrow lazy bootstrap helper.

    Constraints:
    - keep `app = Dash(...)` importable at module scope
    - do not generate the 2M-row simulation dataset during module import
    - preserve current callback behavior by routing all adapter access through a helper such as `get_adapter()` / `ensure_adapter()`
    - keep the change surgical to this phase; do not turn it into a full app-factory rewrite unless the file structure makes the minimal path impossible

    Expected implementation shape:
    - module-scope adapter state starts unset
    - helper initializes adapter and loads initial data on first real use
    - callbacks or data-serving helpers call the bootstrap helper before touching the adapter
  </action>
  <verify>
    <automated>pytest tests/test_frontend_contract.py --collect-only -q</automated>
  </verify>
  <done>`dash_presentation.app` can be imported during collection without generating synthetic data or opening the in-memory DuckDB stack.</done>
</task>

<task type="auto">
  <name>Task 2: Move app import verification behind a test/fixture boundary</name>
  <files>tests/test_frontend_contract.py</files>
  <action>
    Remove module-scope `dash_presentation.app` import usage from the test file and replace it with an explicit test-level or fixture-level import.

    Requirements:
    - preserve the intent of the existing app smoke check
    - assert that the imported object still exposes a layout
    - keep the rest of the contract fixture setup local and independent from `dash_presentation.app`
    - avoid any new global fixture that would reintroduce collection-time work
  </action>
  <verify>
    <automated>pytest tests/test_frontend_contract.py::test_dash_app_import_and_layout_valid -q</automated>
  </verify>
  <done>The contract test still validates the Dash app import/layout path, but pytest collection no longer executes backend bootstrap.</done>
</task>

</tasks>

<verification>
Run the collection gate first, then the targeted contract module:

`pytest tests/test_frontend_contract.py --collect-only -q`

`pytest tests/test_frontend_contract.py -q`

Expected: the file collects without import-time data generation side effects, and the targeted frontend contract tests remain green.
</verification>

<success_criteria>
- `tests/test_frontend_contract.py` collects without module-scope data generation or DuckDB initialization
- `dash_presentation.app` still exposes a valid `app` and layout when imported inside a test
- Adapter/data bootstrap is lazy rather than import-time
- QUAL-05 is fully represented by executable tasks and verification
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-test-harness-hardening/03.2-01-SUMMARY.md`.
</output>
