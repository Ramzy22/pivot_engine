---
phase: 03.2-test-harness-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 03.2-01
files_modified:
  - dash_presentation/app.py
  - tests/test_frontend_contract.py
autonomous: true
requirements:
  - QUAL-06
  - QUAL-05

must_haves:
  truths:
    - "`update_pivot_table` has exactly one terminal `except Exception` block in its main execution path"
    - "Removing the dead duplicate handler does not change callback output arity or fallback semantics"
    - "The collection-safe import changes from Plan 01 still hold after the callback cleanup"
    - "Targeted frontend contract verification remains green after both hardening changes"
  artifacts:
    - path: "dash_presentation/app.py"
      provides: "Callback cleanup with the duplicate terminal exception block removed"
      contains: "def update_pivot_table"
    - path: "tests/test_frontend_contract.py"
      provides: "Ongoing verification that app import/layout and contract flow still work after the callback cleanup"
      exports:
        - test_dash_app_import_and_layout_valid
  key_links:
    - from: "dash_presentation/app.py"
      to: "tests/test_frontend_contract.py"
      via: "targeted contract verification after callback cleanup"
      pattern: "update_pivot_table|app.layout"
---

<objective>
Finish Phase 3.2 by removing the dead duplicate exception handler in `update_pivot_table` and verifying the earlier harness hardening remains intact.

Purpose: satisfy QUAL-06 without expanding into a broader callback refactor, then close the phase with focused verification.
Output: cleaned callback error handling and a green targeted verification pass covering both QUAL-05 and QUAL-06.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.2-test-harness-hardening/03.2-RESEARCH.md
@.planning/phases/03.2-test-harness-hardening/03.2-VALIDATION.md
@.planning/phases/03.2-test-harness-hardening/03.2-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the unreachable duplicate terminal except block from update_pivot_table</name>
  <files>dash_presentation/app.py</files>
  <action>
    Inspect `update_pivot_table` and delete only the duplicate terminal `except Exception` block identified in research.

    Constraints:
    - keep the existing drill-through and cell-update exception handlers unless they directly conflict with the cleanup
    - preserve return shape and existing fallback values
    - do not collapse the callback into one giant try/except as part of this phase

    This task is intentionally narrow: the goal is dead-code removal, not callback redesign.
  </action>
  <verify>
    <automated>rg -n "except Exception" dash_presentation/app.py</automated>
  </verify>
  <done>`update_pivot_table` contains one terminal main-path exception handler and no unreachable duplicate block.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run collection and targeted contract verification for the hardened harness</name>
  <files>dash_presentation/app.py, tests/test_frontend_contract.py</files>
  <action>
    Verify both requirements together after the cleanup:
    - collection remains side-effect free
    - the app import/layout smoke check still works
    - targeted frontend contract tests still pass

    Run the focused commands from the validation strategy rather than the entire repository unless failures suggest broader fallout.
  </action>
  <verify>
    <automated>pytest tests/test_frontend_contract.py --collect-only -q && pytest tests/test_frontend_contract.py -q</automated>
  </verify>
  <done>The hardened test module still collects cleanly and the focused contract checks remain green after the callback cleanup.</done>
</task>

</tasks>

<verification>
Run the focused validation sequence:

`pytest tests/test_frontend_contract.py --collect-only -q`

`pytest tests/test_frontend_contract.py -q`

`rg -n "except Exception" dash_presentation/app.py`

Expected: collection is side-effect free, the targeted contract tests pass, and `update_pivot_table` has only one terminal main-path `except Exception` block.
</verification>

<success_criteria>
- `dash_presentation/app.py` no longer contains the unreachable duplicate terminal `except Exception` block
- Plan 01's import-safety work remains intact after cleanup
- Focused verification proves both QUAL-05 and QUAL-06
- The full phase is executable in two ordered waves with minimal file overlap
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-test-harness-hardening/03.2-02-SUMMARY.md`.
</output>
