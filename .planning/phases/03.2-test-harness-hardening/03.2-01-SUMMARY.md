---
phase: 03.2-test-harness-hardening
plan: 01
subsystem: testing
tags: [pytest, dash, import-safety, lazy-bootstrap, qual-05]

requires:
  - phase: 03.1-debug-instrumentation-grand-total-fix
    provides: stable adapter and virtual-scroll contract baseline

provides:
  - Lazy adapter bootstrap in `dash_presentation/app.py`
  - Collection-safe app import smoke test in `tests/test_frontend_contract.py`
  - Clean pytest collection for the frontend contract module with no import-time data generation

affects:
  - 03.2-02: Plan 02 depends on the collection-safe harness remaining intact during callback cleanup

tech-stack:
  added: []
  patterns:
    - "Lazy singleton bootstrap: module-scope `get_adapter()` initializes backend state on first real use"
    - "Test-local import pattern: import Dash app inside the test function, not at module scope"

key-files:
  created: []
  modified:
    - dash_presentation/app.py
    - tests/test_frontend_contract.py

key-decisions:
  - "Kept `app = Dash(__name__)` at module scope so import and layout checks remain cheap and stable"
  - "Moved backend initialization behind `get_adapter()` instead of introducing a full app factory"
  - "Verified collection through `python -m pytest` because `pytest` is not on PATH in this shell"

patterns-established:
  - "Pattern: Dash presentation modules should avoid module-scope data/bootstrap work when tests import them"

requirements-completed:
  - QUAL-05

duration: 9min
completed: 2026-03-14
---

# Phase 03.2 Plan 01: Import-Safe Test Harness Summary

**The Dash presentation app now imports without bootstrapping DuckDB or generating the 2M-row fixture, and the frontend contract test verifies app import/layout from inside a test rather than at module scope**

## Performance

- **Duration:** 9 min
- **Completed:** 2026-03-14
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Replaced module-scope adapter creation in `dash_presentation/app.py` with a lazy `get_adapter()` path
- Converted `load_initial_data()` to accept the adapter instance created on first use
- Updated `update_pivot_table()` to initialize backend state only when the callback runs
- Removed module-scope `from dash_presentation.app import app` from `tests/test_frontend_contract.py`
- Added `test_dash_app_import_and_layout_valid()` to preserve the original app/layout smoke check without collection-time side effects

## Verification

- `python -m pytest tests/test_frontend_contract.py --collect-only -q`
- `python -m pytest tests/test_frontend_contract.py::test_dash_app_import_and_layout_valid -q`

Both commands passed. The contract module collects 10 tests in under 2 seconds, and the app import/layout smoke test passes green.

## Files Created/Modified

- `dash_presentation/app.py` - lazy backend bootstrap via `get_adapter()`
- `tests/test_frontend_contract.py` - test-level app import smoke check

## Decisions Made

- Used a module-level lazy singleton instead of a broader app-factory rewrite to keep the phase surgical
- Left `app.layout` unchanged so existing UI wiring and smoke expectations remain stable
- Kept the contract-test fixture independent from `dash_presentation.app`

## Deviations from Plan

### Deviation: Verification command runner

- **Issue:** The plan specified `pytest ...`, but `pytest` is not available on PATH in this shell.
- **Fix:** Ran the equivalent commands with `python -m pytest`.
- **Verification:** Both plan checks passed with identical test targets.

## User Setup Required

None.

## Next Phase Readiness

- The harness is now collection-safe for the callback cleanup in Plan 02
- `dash_presentation.app` remains importable and exposes a valid layout

## Self-Check: PASSED

- FOUND: `dash_presentation/app.py`
- FOUND: `tests/test_frontend_contract.py`
- VERIFIED: collection is side-effect free for `tests/test_frontend_contract.py`

---
*Phase: 03.2-test-harness-hardening*
*Completed: 2026-03-14*
